---
phase: 03-forms
plan: 11
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FRM-06]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FRM-06 in `.planning/REQUIREMENTS.md` states what the code recovers: which event slots the compiled form binds, the index of each slot, and the native address of each bound handler."
    - "FRM-06 states plainly that it does not recover the name of an event, and it cites decision D-02 as the reason."
    - "The Phase 3 goal in `.planning/ROADMAP.md` closes on event structure, not on a name."
    - "A success criterion in `.planning/ROADMAP.md` Phase 3 names event structure and can be checked against a live `inspect` run."
    - "The Phase 3 named risk that asks for a second derived name table is marked withdrawn, in the same shape the roadmap already uses for the risk that D-01 withdrew."
    - "Neither file asks for a capability that D-02 forbids this repository to build."
  prohibitions:
    - statement: "No task in this plan changes a source file. This plan changes documentation only. A code change and a documentation change never share a commit."
    - statement: "No task in this plan plans, proposes, or leaves a seam for a name table derived from `VB6.OLB`. D-02 is locked and this plan does not re-open it."
    - statement: "No task weakens a requirement below what the shipped code already proves. The amended text describes the capability that `controlinfo.rs` already recovers and that plan 03-09 already tested."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "an FRM-06 bullet that names event structure and cites D-02"
      contains: "D-02"
    - path: ".planning/ROADMAP.md"
      provides: "a Phase 3 goal and a Phase 3 success criterion that name event structure"
      contains: "event structure"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03-forms/03-CONTEXT.md"
      via: "the amended FRM-06 text cites decision D-02 by name at the amendment site"
      pattern: "D-02"
    - from: ".planning/ROADMAP.md"
      to: "crates/deform6/src/vb/controlinfo.rs"
      via: "the amended success criterion names the three facts report_events already gives"
      pattern: "event slot"
---

<objective>
Amend FRM-06 and the Phase 3 goal so both describe the capability this
repository is allowed to build and already built.

Purpose: the phase 3 verification scored FRM-06 as failed. The code is not at
fault. `EventNameTable` ships with no entries because decision D-02 bars a
name table derived from `VB6.OLB`, and the compiled file holds no name of its
own. FRM-06 as written can therefore never pass, in this phase or in any
later one. The requirement is wrong, not the code. What the code does recover
is event structure: which slot the form binds, the index of that slot, and
the native address of the handler behind it. Plan 03-09 built that, tested it,
and cross-checked it. This plan makes the written requirement say so.

Output: an amended FRM-06 bullet in `.planning/REQUIREMENTS.md`, and an
amended Phase 3 goal, success criteria and named risk in
`.planning/ROADMAP.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-forms/03-CONTEXT.md
@.planning/phases/03-forms/03-VERIFICATION.md
@.planning/phases/03-forms/03-09-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces no new symbol. It changes two documents.

| Document | Section changed |
|---|---|
| `.planning/REQUIREMENTS.md` | the FRM-06 bullet under `### Forms` |
| `.planning/ROADMAP.md` | the Phase 3 overview sentence, the Phase 3 list entry, the Phase 3 goal, the Phase 3 success criteria, the Phase 3 named risk about a second derived table |

## The decision this plan carries out

The human took this decision directly, after reading the verification report.
It is locked. Do not re-open it and do not plan the alternative.

> Decision D-02 stands. This repository does not derive an event name table
> from `VB6.OLB`. FRM-06 as written can never pass. Narrow the requirement so
> it describes event structure: which slots are bound, their index, and the
> handler address. State the reason and cite D-02 at the amendment site. Plan
> no parser work, no name table, and no research into name sources.

<tasks>

<task type="auto">
  <name>Task 1: Narrow FRM-06 in REQUIREMENTS.md and cite D-02</name>
  <read_first>
    - `.planning/REQUIREMENTS.md`, the `### Forms` section and the `## Traceability` table, in full. Both mention FRM-06.
    - `.planning/phases/03-forms/03-CONTEXT.md`, decision D-02, in full. It is the reason this amendment exists.
    - `.planning/phases/03-forms/03-09-SUMMARY.md`, the `provides` block and the `coverage` block. They name the three facts the code recovers today. Use those names, not invented ones.
    - `crates/deform6/src/vb/controlinfo.rs`, the doc comment on `report_events` and the `EventReport` enum. Read what each variant carries. The amended requirement must describe these values and nothing more.
    - `AGENTS.md`, "How to write". Every sentence you add follows it.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Replace the FRM-06 bullet under `### Forms` with a bullet that states
    three things.

    First, what the tool recovers: the event structure of each control. Name
    the three facts `report_events` gives, using the names
    `crates/deform6/src/vb/controlinfo.rs` already uses for them: which event
    slots the compiled form binds, the index of each slot, and the native
    address of each bound handler.

    Second, what the tool does not recover: the name of an event. Say it in
    one plain sentence.

    Third, why. The compiled file holds no such name. Deriving one needs a
    table built from a Microsoft type library, and decision D-02 in
    `.planning/phases/03-forms/03-CONTEXT.md` bars that table from this
    repository, because `AGENTS.md` bars a fixture calculated from a third
    party file. Name `D-02` and name the file that holds it, so a reader six
    months from now finds the reasoning without asking anybody.

    Keep the bullet's checkbox state consistent with the rest of the Forms
    section as you find it. Do not mark FRM-06 complete in this task: plan
    03-09 already built the capability, but the `## Traceability` table's
    Phase 3 row is settled by the phase verification pass, not by this plan.
    Leave the Traceability table alone.

    Add nothing else. Do not add a new requirement id. Do not move FRM-06 to
    the v2 list. The capability exists and ships today; only its written
    description was wrong.

    Commit this task on its own. The subject line says what the change does,
    in the present tense, and names no tool and no agent.
  </action>
  <verify>
    <automated>grep -c 'D-02' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>grep -A 8 'FRM-06' .planning/REQUIREMENTS.md | grep -c 'event structure'</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>! grep -q 'event handler names' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, which means the withdrawn wording is still in the file</fails_when>
    <automated>git show --name-only --format= HEAD</automated>
    <fails_when>the output names any path under crates/, or names more than one file</fails_when>
  </verify>
  <acceptance_criteria>
    - The FRM-06 bullet names which event slots are bound, the slot index, and the handler address.
    - The FRM-06 bullet states in one sentence that the tool does not recover the name of an event.
    - The FRM-06 bullet names `D-02` and names `.planning/phases/03-forms/03-CONTEXT.md`.
    - The withdrawn wording is gone from the whole file. Source assertion: `! grep -q 'event handler names' .planning/REQUIREMENTS.md`.
    - The commit touches exactly one file and no file under `crates/`.
    - Every sentence added is Simplified Technical English. Source assertion: `! grep -q $'\xe2\x80\x94' .planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>FRM-06 describes a capability this repository is allowed to build, names the three facts it recovers, and cites the decision that narrowed it.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the Phase 3 goal, success criteria and named risk in ROADMAP.md</name>
  <read_first>
    - `.planning/ROADMAP.md` lines 1 to 40, for the Overview paragraph and the Phase 3 list entry. Both carry the withdrawn wording.
    - `.planning/ROADMAP.md`, the whole `### Phase 3: Forms` section, from the `**Goal**` line to the `**Plans**` line. You change the goal, the success criteria and one named risk inside it.
    - `.planning/ROADMAP.md`, the Phase 3 note that records how D-01 withdrew the gap 15 named risk. Copy that shape for D-02. The roadmap already has a pattern for a withdrawn risk; follow it rather than inventing a second pattern.
    - `.planning/REQUIREMENTS.md`, the FRM-06 bullet as task 1 left it. The roadmap and the requirements must agree word for word on what is recovered.
    - `crates/deform6-cli/src/main.rs`, the `print_event` function. A success criterion must be checkable against what a live run prints, so read what it prints before you write the criterion.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Change four places in `.planning/ROADMAP.md`. Use scoped edits. Never
    rewrite the whole file.

    First, the Overview paragraph's Phase 3 sentence, and the Phase 3 entry in
    the phase list. Both close on the withdrawn wording. Close both on event
    structure instead, in the same compressed style the neighbouring phase
    entries use.

    Second, the `**Goal**` line of `### Phase 3: Forms`. Keep every other
    clause exactly as it stands: the control tree, the parent, the type and
    name, the property values, the OCX CLSID and the resource blobs are all
    unchanged. Replace only the closing clause so the goal asks for the event
    structure of each control: which event slots are bound, the index of each
    slot, and the address of each bound handler.

    Third, the `**Success Criteria**` list. It has five entries today and none
    of them covers events at all, which is why the verification pass had to
    score the goal's own closing clause with no criterion behind it. Add a
    sixth criterion that a person can check against a live run. State that
    `deform6 inspect` on a corpus program that binds an event prints, for
    every slot of every control, the slot index and whether that slot is
    bound, and that a slot with no name says so plainly rather than printing a
    guess. Name the corpus program you checked it against. Do not name a
    program you did not run.

    Fourth, the named risk that asks for a second derived data table for
    events. Mark it withdrawn and give the reason, in the same shape the
    Phase 3 note already uses for the risk D-01 withdrew: name the decision,
    name the rule in `AGENTS.md` behind it, and say what the phase delivers
    instead. Do not delete the risk text. A reader needs to see that the risk
    was considered and answered, not that it vanished.

    Do not touch any other phase. Do not touch the Phase 3 plan list or the
    wave note; a later step of this gap run owns those.

    Commit this task on its own, separate from task 1. Two documents changed
    for the same reason still make two commits, because a reader removes one
    and keeps the other.
  </action>
  <verify>
    <automated>! grep -q 'event handler names' .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit, which means the withdrawn wording is still in the file</fails_when>
    <automated>sed -n '/### Phase 3: Forms/,/### Phase 4/p' .planning/ROADMAP.md | grep -c 'event structure'</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>sed -n '/### Phase 3: Forms/,/### Phase 4/p' .planning/ROADMAP.md | grep -c 'D-02'</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>sed -n '/### Phase 3: Forms/,/\*\*Named risks\*\*/p' .planning/ROADMAP.md | grep -cE '^  6\.'</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>git show --name-only --format= HEAD</automated>
    <fails_when>the output names any path under crates/, or names more than one file</fails_when>
  </verify>
  <acceptance_criteria>
    - The Phase 3 goal closes on event structure and every other clause of the goal is unchanged.
    - A sixth success criterion exists under Phase 3, and it names a corpus program the executor actually ran `deform6 inspect` against.
    - The Phase 3 section cites `D-02` at the amendment site.
    - The named risk about a second derived table is marked withdrawn and its original text is still readable.
    - The withdrawn wording is gone from the whole file. Source assertion: `! grep -q 'event handler names' .planning/ROADMAP.md`.
    - The Overview paragraph and the Phase 3 list entry agree with the amended goal.
    - The commit touches exactly one file and no file under `crates/`.
    - No em-dash enters the file. Source assertion: `! grep -q $'\xe2\x80\x94' .planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>The roadmap asks Phase 3 for a capability this repository is allowed to build, and the sixth criterion can be checked by running the tool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document to reader | A requirement document is the project's own statement of what it delivers. A reader trusts it without reading the code. |
| document to later planner | A phase 4 planner reads `ROADMAP.md` and plans work from it. Wrong text there becomes wrong work later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-57 | Spoofing | An amended requirement claiming more than the code delivers | high | mitigate | The amended text names only the three facts `report_events` gives today, read from `controlinfo.rs` before the text is written, and the sixth success criterion is checked against a live `inspect` run on a named corpus program. |
| T-03-58 | Repudiation | A narrowed requirement with no recorded reason | high | mitigate | The amendment cites `D-02` and names the file that holds it, in both documents, so the narrowing is traceable to a human decision and not to an agent's convenience. |
| T-03-59 | Tampering | A whole-file rewrite of `ROADMAP.md` destroying phases outside the edit | high | mitigate | Every change is a scoped edit inside the Phase 3 section, the Overview paragraph, or the phase list. The task forbids a whole-file write and the verify step asserts one file per commit. |
| T-03-60 | Information Disclosure | A documentation edit smuggling a source change into the same commit | medium | mitigate | Each task's verify step runs `git show --name-only --format= HEAD` and fails when the output names any path under `crates/`. |

No package manager install happens in this plan, so no supply chain row
applies.
</threat_model>

<verification>
This plan changes documentation only. The source gate still runs before every
commit, because `AGENTS.md` states it for every commit and not for a
selection:

    cargo fmt --all --check
    cargo clippy --all-targets -- -D warnings
    cargo test --workspace

Then confirm both documents agree with each other and with the code:

    grep -A 8 'FRM-06' .planning/REQUIREMENTS.md
    sed -n '/### Phase 3: Forms/,/### Phase 4/p' .planning/ROADMAP.md
    ./target/debug/deform6 inspect corpus/vb6-code/Fire-effect/Fast_Flames.exe | grep 'event slot' | head
</verification>

<success_criteria>
- FRM-06 describes event structure and cites D-02.
- The Phase 3 goal describes event structure and every other clause is unchanged.
- A sixth Phase 3 success criterion exists and names a program the executor ran.
- The withdrawn named risk is marked withdrawn with its reason, and its text is still readable.
- Two commits exist. Neither touches a file under `crates/`.
- Every one of the three gate commands passes before every commit.
</success_criteria>

<output>
Create `.planning/phases/03-forms/03-11-SUMMARY.md` when done.
</output>

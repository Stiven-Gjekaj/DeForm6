---
phase: 03-forms
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/phases/03-forms/03-RESEARCH.md
autonomous: true
requirements: [FRM-01, FRM-03]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`03-RESEARCH.md` no longer states the `Length - 2` property-loop bound as a confirmed formula. It records that plan 03-06 measured `Length - 1` against the shipped tiling accounting and kept `Length - 1`."
    - "`03-RESEARCH.md`'s position-block escape passage no longer contains code that reads eighteen bytes while returning sixteen as its consumed count."
    - "`03-RESEARCH.md`'s scope-run passage no longer presents `0x02` and `0x03` as a symmetric pair. It records what plan 03-04 measured across four real transitions in one corpus program."
    - "Every correction names the SUMMARY that proved it, so a reader traces the number back to the measurement that produced it."
    - "The disproven text stays readable next to the correction. A reader sees that the claim was tested and answered, not that it disappeared."
    - "A phase 4 planner who reads only `03-RESEARCH.md` implements the formulas that hold against real corpus bytes."
  prohibitions:
    - statement: "No task in this plan changes a source file. This plan changes one document. A code change and a documentation change never share a commit."
    - statement: "No correction states a number this repository has not measured. Every corrected figure comes from a committed SUMMARY that names the corpus file it was measured in."
    - statement: "No correction deletes the original claim. The original text stays, marked as disproven, with the measurement that disproved it beside it."
    - statement: "No task re-runs the research. The three measurements are already committed. This plan copies them into the document that lacks them."
  artifacts:
    - path: ".planning/phases/03-forms/03-RESEARCH.md"
      provides: "three corrections, each citing the SUMMARY that measured it"
      contains: "03-06-SUMMARY.md"
  key_links:
    - from: ".planning/phases/03-forms/03-RESEARCH.md"
      to: ".planning/phases/03-forms/03-04-SUMMARY.md"
      via: "the scope-run correction cites the four measured transitions in Grayscale.exe"
      pattern: "03-04-SUMMARY"
    - from: ".planning/phases/03-forms/03-RESEARCH.md"
      to: ".planning/research/STRUCTURES.md"
      via: "the scope-run passage points at STRUCTURES.md section 8.9 as the authoritative statement of the grammar"
      pattern: "STRUCTURES.md"
---

<objective>
Correct the three passages in `03-RESEARCH.md` that three plans of this phase
disproved against real corpus bytes and never fed back.

Purpose: `03-RESEARCH.md` is stale. Plan 03-04 found that the scope-byte
`0x02`/`0x03` symmetry the document implies does not hold. Plan 03-06 found
that the section 8.3 `Length - 2` property-loop bound does not reconcile
against the tiling accounting `controltree.rs` already shipped, and that the
document's own position-block escape code reads eighteen bytes while claiming
to return sixteen. All three plans worked around the document and none of them
corrected it. Phase 4 reads this document. A phase 4 planner who reads only
this document implements the disproven bound.

Output: an amended `.planning/phases/03-forms/03-RESEARCH.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-forms/03-VERIFICATION.md
@.planning/phases/03-forms/03-04-SUMMARY.md
@.planning/phases/03-forms/03-06-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces no new symbol. It corrects three passages in one document.

| Passage | What is wrong | Who disproved it |
|---|---|---|
| The zero-children reconciliation, around line 523 | It states `blockStart + Length - 2` and `blockStart + Length + 2` as internally consistent confirmed formulas | plan 03-06, against the shipped `Length - 1` tiling accounting |
| The illustrative `read_position_block`, around line 880 | The `-32768` branch reads 2 + 16 = 18 bytes and returns `16` as the consumed count | plan 03-06, by reading the document's own code |
| The illustrative `read_scope_run`, around line 571 | It matches `0x02 | 0x03` in one arm, which presents them as a symmetric pair | plan 03-04, across four real transitions in `Grayscale.exe` |

## What this plan does not touch

`.planning/research/STRUCTURES.md` is the authoritative research document and
a later plan of this gap run owns it. This plan does not edit it. Where a
corrected passage needs a reader to go somewhere authoritative, point at
`STRUCTURES.md` section 8.9 rather than restating the grammar here. A second
statement of a grammar in a second document is a second thing to keep in
sync, and this plan exists because that already failed once.

<tasks>

<task type="auto">
  <name>Task 1: Correct the property-loop bound in the zero-children reconciliation</name>
  <read_first>
    - `.planning/phases/03-forms/03-RESEARCH.md` lines 500 to 560, in full. This is the passage you change. Read the hex dump above it too, because the corrected sentence must still agree with the bytes printed there.
    - `.planning/phases/03-forms/03-06-SUMMARY.md`, the `key-decisions` block. It states what 03-06 measured and why it kept `Length - 1`. Quote the figure from there, not from memory.
    - `crates/deform6/src/vb/propstream.rs`, the first fifteen lines of `walk_properties`. Read the line that computes `block_end`. That is the bound the code actually ships.
    - `crates/deform6/src/vb/controltree.rs`, the doc comment that explains where a control block's own separator starts. It gives the `Length - 1` accounting the property loop reconciles against.
    - `AGENTS.md`, "What to measure". The corrected text gives the number that can be proved and names where it was proved.
  </read_first>
  <files>.planning/phases/03-forms/03-RESEARCH.md</files>
  <action>
    Amend the sentence that calls the two formulas "internally consistent for
    the zero-children case".

    Keep the original sentence. Add a correction note directly beneath it.
    The note states four things. First, that the claim holds only for the
    zero-children sample the dump above it shows, and does not generalise.
    Second, the bound the shipped code uses, read from
    `crates/deform6/src/vb/propstream.rs`, with the exact expression that file
    computes. Third, that plan 03-06 measured this and kept the shipped bound,
    with `03-06-SUMMARY.md` named as the source. Fourth, that a reader
    implementing a property loop uses the shipped bound and not the formula in
    the original sentence.

    Mark the original sentence as disproven in the general case. Do not delete
    it. A reader has to see that the formula was tested, because a deleted
    claim looks like a claim nobody ever made, and the next researcher tests
    it again.

    Do not restate the grammar. Do not add a second worked example. Add the
    note and nothing else.

    Commit this task on its own.
  </action>
  <verify>
    <automated>grep -c '03-06-SUMMARY' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>grep -n 'Length - 1' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>no line is printed, or non-zero exit</fails_when>
    <automated>grep -c 'Length - 2' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>the printed count is 0, which means the original claim was deleted instead of marked</fails_when>
    <automated>git show --name-only --format= HEAD</automated>
    <fails_when>the output names any path under crates/, or names more than one file</fails_when>
  </verify>
  <acceptance_criteria>
    - The corrected note names the exact bound expression `crates/deform6/src/vb/propstream.rs` computes.
    - The corrected note names `03-06-SUMMARY.md` as the source of the measurement.
    - The original sentence is still in the file and is marked as disproven in the general case.
    - The commit touches exactly one file and no file under `crates/`.
    - No em-dash enters the file. Source assertion: `! grep -q $'\xe2\x80\x94' .planning/phases/03-forms/03-RESEARCH.md`.
  </acceptance_criteria>
  <done>A reader of this passage implements the bound the code ships and can trace it to the plan that measured it.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the position-block escape's own byte count</name>
  <read_first>
    - `.planning/phases/03-forms/03-RESEARCH.md` lines 865 to 905, in full. This is the illustrative `read_position_block` you change.
    - `.planning/phases/03-forms/03-06-SUMMARY.md`, the deviation that names this contradiction. It states which of the two numbers 03-06 implemented and why.
    - `crates/deform6/src/vb/propstream.rs`, `read_position_block`, in full. Read what the shipped function returns as its consumed count for the escape branch.
    - `.planning/research/STRUCTURES.md` section 8.5.1, the paragraph on the position block. It is the upstream statement the illustrative code was derived from. Read it before you decide which number is right.
  </read_first>
  <files>.planning/phases/03-forms/03-RESEARCH.md</files>
  <action>
    The illustrative `read_position_block` in this document contradicts
    itself. Its escape branch starts two bytes past `at` and then reads four
    four-byte values, which is eighteen bytes from `at`, and it returns `16`
    as the count of bytes consumed. Nothing outside the document is needed to
    see this. It is an internal contradiction in the code sample.

    Correct the sample so it agrees with itself and with the shipped
    `read_position_block`. Read the shipped function first and match its
    reading, because the shipped one is the one that passes against real
    corpus bytes. State in one sentence beneath the sample which reading the
    code ships and that plan 03-06 settled it, naming `03-06-SUMMARY.md`.

    Keep the comment above the sample that says it is illustrative only and
    that the executor writes the real implementation. That comment is correct
    and it is the reason a wrong sample is a documentation defect rather than
    a code defect.

    Do not change the short branch. It is correct.

    Commit this task on its own.
  </action>
  <verify>
    <automated>grep -c 'checked_add(12)' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>grep -c '03-06-SUMMARY' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>the printed count is less than 2, or non-zero exit</fails_when>
    <automated>git show --name-only --format= HEAD</automated>
    <fails_when>the output names any path under crates/, or names more than one file</fails_when>
  </verify>
  <acceptance_criteria>
    - The escape branch's returned count and the bytes it reads agree with each other.
    - The escape branch's reading matches `crates/deform6/src/vb/propstream.rs`'s shipped `read_position_block`. The executor states in the SUMMARY which of the two numbers the shipped code uses and quotes the line it read that from.
    - A sentence beneath the sample names `03-06-SUMMARY.md` as the plan that settled it.
    - The comment marking the sample as illustrative is still present.
    - The commit touches exactly one file and no file under `crates/`.
  </acceptance_criteria>
  <done>The document's own position-block sample no longer contradicts itself, and it agrees with the function that ships.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the scope-run sample and point at the authoritative grammar</name>
  <read_first>
    - `.planning/phases/03-forms/03-RESEARCH.md` lines 555 to 600, in full. This is the illustrative `read_scope_run` you change.
    - `.planning/phases/03-forms/03-04-SUMMARY.md`, the `key-decisions` block, in full. It names the four real transitions in `corpus/vb6-code/Grayscale-effect/Grayscale.exe` that settled the grammar, and it names the menu special case.
    - `crates/deform6/src/vb/controltree.rs`, `read_scope_run` and the `ScopeRun` enum, in full. This is the reading that ships and passes.
    - `.planning/research/STRUCTURES.md` section 8.9. It is the authoritative statement of this grammar and the place a reader must be sent.
    - `.planning/WINDOWS.md`, finding 7. One transition of this grammar is still open. The corrected text must say so rather than imply the grammar is settled.
  </read_first>
  <files>.planning/phases/03-forms/03-RESEARCH.md</files>
  <action>
    The illustrative `read_scope_run` puts `0x02` and `0x03` in one match arm.
    That presents them as a symmetric pair. Plan 03-04 measured four real
    transitions in `Grayscale.exe` and found they are not symmetric, and it
    found a further reading for a menu control that the sample does not have
    at all.

    Do not rewrite the grammar inside this document. Do three things instead.

    First, correct the sample so it no longer joins the two bytes in one arm,
    and so it agrees with the `ScopeRun` variants
    `crates/deform6/src/vb/controltree.rs` actually returns. Read that file
    before you write the sample.

    Second, add one sentence naming `03-04-SUMMARY.md` and the corpus program
    the four transitions were measured in, so the correction carries its own
    evidence.

    Third, add one sentence sending the reader to `STRUCTURES.md` section 8.9
    as the authoritative statement of this grammar, and one sentence recording
    that `.planning/WINDOWS.md` finding 7 leaves one transition of it open.
    A reader must not close this document believing the grammar is complete.

    Keep the `MAX_SCOPE_RUN` bound and the sentence that calls it defensive.
    Both are correct and the shipped code keeps them.

    Commit this task on its own.
  </action>
  <verify>
    <automated>sed -n '/fn read_scope_run/,/### Pattern 3/p' .planning/phases/03-forms/03-RESEARCH.md | grep -c 'ScopeRun::'</automated>
    <fails_when>the printed count is less than 4, or non-zero exit</fails_when>
    <automated>grep -c '03-04-SUMMARY' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>grep -c 'WINDOWS.md' .planning/phases/03-forms/03-RESEARCH.md</automated>
    <fails_when>the printed count is 0, or non-zero exit</fails_when>
    <automated>sed -n '/fn read_scope_run/,/### Pattern 3/p' .planning/phases/03-forms/03-RESEARCH.md | grep -c 'MAX_SCOPE_RUN'</automated>
    <fails_when>the printed count is 0, which means the defensive bound was removed</fails_when>
    <automated>git show --name-only --format= HEAD</automated>
    <fails_when>the output names any path under crates/, or names more than one file</fails_when>
  </verify>
  <acceptance_criteria>
    - The two scope bytes no longer share one match arm in the sample.
    - The sample's variants agree with the `ScopeRun` variants `crates/deform6/src/vb/controltree.rs` returns. The executor lists both sets in the SUMMARY and states that they match.
    - A sentence names `03-04-SUMMARY.md` and names `corpus/vb6-code/Grayscale-effect/Grayscale.exe`.
    - A sentence sends the reader to `STRUCTURES.md` section 8.9.
    - A sentence records that `.planning/WINDOWS.md` finding 7 leaves one transition open.
    - The `MAX_SCOPE_RUN` bound and the sentence calling it defensive are still present.
    - The commit touches exactly one file and no file under `crates/`.
  </acceptance_criteria>
  <done>The scope-run sample agrees with the shipped reader, carries its own evidence, and sends a reader to the authoritative document rather than competing with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document to later planner | A phase 4 planner reads this document and plans work from it. A disproven formula here becomes wrong code later. |
| document to document | This document and `STRUCTURES.md` both describe the same structures. Two statements of one grammar drift apart. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-61 | Tampering | A corrected figure taken from memory rather than from a measurement | high | mitigate | Every corrected figure is read out of a committed SUMMARY or out of the shipped source file, and the correction names which one. Each task's `read_first` names the exact file and section to read first. |
| T-03-62 | Repudiation | A disproven claim deleted rather than marked | medium | mitigate | Each task keeps the original text and marks it. The verify step for task 1 asserts the original formula string is still present in the file. |
| T-03-63 | Spoofing | This document restating a grammar and drifting from `STRUCTURES.md` again | high | mitigate | Task 3 forbids restating the grammar here. It points the reader at `STRUCTURES.md` section 8.9 and records the one transition that is still open. |
| T-03-64 | Information Disclosure | A documentation edit smuggling a source change into the same commit | medium | mitigate | Each task's verify step runs `git show --name-only --format= HEAD` and fails when the output names any path under `crates/`. |

No package manager install happens in this plan, so no supply chain row
applies.
</threat_model>

<verification>
This plan changes documentation only. The source gate still runs before every
commit, because `AGENTS.md` states it for every commit and not for a
selection:

    cargo fmt --all --check
    cargo clippy --all-targets -- -D warnings
    cargo test --workspace

Then confirm the three corrections landed and each carries its evidence:

    grep -n 'Length - 1' .planning/phases/03-forms/03-RESEARCH.md
    grep -n '03-04-SUMMARY\|03-06-SUMMARY' .planning/phases/03-forms/03-RESEARCH.md
    grep -n 'WINDOWS.md' .planning/phases/03-forms/03-RESEARCH.md
</verification>

<success_criteria>
- The property-loop bound passage names the bound the code ships and cites 03-06.
- The position-block sample reads the same number of bytes it says it consumes.
- The scope-run sample no longer joins the two scope bytes in one arm, cites 03-04, and points at `STRUCTURES.md` section 8.9.
- Every original claim is still readable and marked, not deleted.
- Three commits exist. None touches a file under `crates/`.
- Every one of the three gate commands passes before every commit.
</success_criteria>

<output>
Create `.planning/phases/03-forms/03-12-SUMMARY.md` when done.
</output>

---
phase: 06-version-1-0
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
autonomous: true
requirements: [DET-05, FRM-01, FRM-02, FRM-03, FRM-04, VER-06, WRT-03]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 64000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A person who reads README.md learns what DeForm6 returns and what it does not return, before they run it."
    - "README.md defines the three confidence words and no fourth tier and no numeric score (FRM-04)."
    - "README.md states that the P-code branch of lpNativeCode is untested, and why: every corpus program is native (DET-05)."
    - "README.md states that corpus/vb6-code/Hidden-Markov-model/frmHMM.frx is damaged upstream and is excluded by name (VER-06)."
    - "README.md states that full recompilation did not run, in the exact words the report's own limits array already uses (WRT-03)."
    - "README.md lists every still-open row of STRUCTURES section 11 and every item of FILE-FORMATS section 9, with the safe default the tool chose for each (FRM-01, FRM-02, FRM-03, WRT-03)."
    - "Length and equality are measured in encoded bytes, never in code points or grapheme clusters. The run assumes a Western code page, replaces every character above U+00FF with a question mark and reports it, and never guesses a different code page. README.md repeats that sentence from the report's own limits array. This answers the `encoding` probe row for all 19 requirements that raised one: DET-03, DET-04, DET-06, FRM-01, FRM-02, FRM-03, FRM-04, FRM-06, OBJ-01, OBJ-03, OBJ-04, OBJ-06, RPT-04, SAF-02, SAF-04, VER-03, VER-05, VER-06, WRT-03."
    - "One run is one process over one file. The library never opens a file, the command line crate owns the file system, and `extract` plans every write and refuses the whole run before it writes anything when a planned path would leave the output directory or a symbolic link already sits at a planned path. An interrupted run leaves a part written directory and the tool claims no atomicity. README.md states this. This answers the `concurrency` probe row for all 12 requirements that raised one: DET-01, DET-04, DET-05, FRM-06, OBJ-05, SAF-02, SAF-03, SAF-04, VER-02, VER-03, WRT-04, WRT-05."
    - "README.md holds no character outside printable ASCII, so it carries no em dash and no emoji, as AGENTS.md requires."
  prohibitions:
    - statement: "README.md must not state a recovery figure as a share of one hundred, and must not state a capability that no measured result supports."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "README.md must not omit a still-open gap to make the tool look more complete than the two registers say it is."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "README.md must not present a result measured on a synthetic fixture as a result measured on the corpus."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "README.md"
      provides: "The released claim surface: what the tool returns, what it does not, the confidence vocabulary, and every open limit"
      min_lines: 150
      contains: "unrecoverable"
  key_links:
    - from: "README.md"
      to: ".planning/research/STRUCTURES.md"
      via: "one README limit row per still-open gap register row, keyed S-NN to the register row number"
      pattern: "^\\| S-[0-9]{2} \\|"
    - from: "README.md"
      to: ".planning/research/FILE-FORMATS.md"
      via: "one README limit row per section 9 gap item, keyed F-NN to the item number"
      pattern: "^\\| F-[0-9]{2} \\|"
    - from: "README.md"
      to: "crates/deform6/src/report.rs"
      via: "the recompilation limit sentence is reused verbatim from build_limits"
      pattern: "needs the Visual Basic 6 IDE on"
---

<objective>
Write `README.md`. State what DeForm6 returns, what it does not return, the three
confidence words, the three facts a reader must have before they run the tool,
and every limit that is still open at release.

Purpose: success criteria 3 and 4. There is no `README.md` anywhere in this
repository today, so the whole file is new work. The roadmap's own named risk is
that the README is the last place a claim can drift, so this plan writes the
prose and the limits, and defers every corpus-measured number to plan 06-07,
which fills them in after the acceptance run measures them.

Output: `README.md`, with five deferred number markers for plan 06-07 to replace.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@.planning/phases/06-version-1-0/06-RESEARCH.md
@.planning/phases/06-version-1-0/06-PATTERNS.md
</context>

<!-- planner-discipline-allow: S-01\|S-11\|S-19 -->

## Artifacts this phase produces

These symbols do not exist in the tree yet. A drift check must not flag them as
unresolved references.

| Artifact | Kind | Plan |
|---|---|---|
| `README.md` | new file | 06-02 |
| `CHANGELOG.md` | new file | 06-05 |
| `LICENSES.md` | new file | 06-05 |
| `crates/deform6/schema/report.schema.json` | new file | 06-01 |
| `crates/deform6/tests/schema.rs` | new file | 06-01 |
| `crates/deform6/tests/gitattributes.rs` | new file | 06-04 |
| `crates/xtask/src/licences.rs` | new file | 06-05 |
| `scripts/check-claim-surface.sh` | new file | 06-06 |
| `scripts/check-release-version.sh` | new file | 06-04 |
| `jsonschema` dev-dependency | new dependency | 06-01 |
| `cargo run -p xtask -- licences` | new xtask subcommand | 06-05 |
| `cargo doc` | new `gate.yml` step | 06-03 |
| `Check the claim surface` | new `gate.yml` step | 06-06 |
| `every_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `the_schema_refuses_a_doctored_report` | new test fn | 06-01 |
| `one_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `every_extension_the_writer_emits_is_named_in_gitattributes` | new test fn | 06-04 |
| `v1.0.0` | new git tag | 06-07 |

<tasks>

<task type="auto">
  <name>Task 1: The README spine, the vocabulary, and the three facts</name>
  <files>README.md</files>
  <read_first>
    - `AGENTS.md` sections "How to write" and "What this project is". The four
      sentences under "What this project is" are the voice and the claim this
      README extends. Do not weaken them.
    - `.planning/PROJECT.md` (the scope and the decisions)
    - `crates/deform6/src/report.rs` lines 74-110 (the `Confidence` enum, its three
      variants, their doc comments, and the `#[serde(rename_all = "lowercase")]`
      attribute that fixes the three JSON words) and lines 367-396 (`build_limits`,
      which already holds the exact recompilation sentence and the exact code page
      sentence in words the README reuses verbatim)
    - `crates/deform6/tests/support/frm.rs` lines 300-320 (the `EXCLUSIONS` table
      and the verbatim reason for the `frmHMM.frx` exclusion)
    - `crates/deform6/src/vb/project.rs` (the doc comments that record why the
      P-code branch of `lpNativeCode` is untested)
    - `crates/deform6-cli/src/main.rs` (the `clap` doc comments that produce the
      `--help` text, so the README describes the same two subcommands the tool
      actually offers, `inspect` and `extract`, and no third one)
    - `corpus/NOTICES` (the shape this repository already uses to turn measured
      facts into short structured prose: a lead sentence, then flat concrete
      facts, each traceable to a named source)
    - `.planning/phases/06-version-1-0/06-PATTERNS.md` section `README.md`
  </read_first>
  <action>
    Create `README.md` at the repository root. Write every sentence in ASD-STE100
    Simplified Technical English, matching `AGENTS.md`: short sentences, active
    voice, present tense, one instruction per sentence, no em dash, no emoji. The
    file holds printable ASCII only.

    Give it these sections, in this order, with these exact headings:

    `# DeForm6`
    A lead paragraph of four sentences taken from `AGENTS.md`'s "What this project
    is": it reads a compiled Visual Basic 6 executable and writes back a Visual
    Basic project; the first milestone recovers the metadata only, the forms, the
    control trees, the property values, the names and the procedure signatures; it
    does not recover statements.

    `## What version 1.0 returns`
    A flat list of what the tool produces: a `.vbp` project file, a `.frm` per
    form, a `.frx` per form that carries a resource blob, a `.bas` per standard
    module, a `.cls` per class module, and one JSON report beside the project. Name
    the two subcommands, `inspect` and `extract`, with one sentence each, matching
    the `--help` text word for word in meaning.

    `## What version 1.0 does not return`
    State plainly, one sentence each: it does not recover statements; it does not
    produce Basic source that a compiler accepts as the original program's
    behaviour; it does not open the Visual Basic 6 IDE and it does not compile
    anything. State no figure as a share of one hundred anywhere in this file.
    Every capability sentence in this README must trace to a `tests/ratios.toml`
    total, a `Confidence` variant name, or a `limits` string the report emits
    verbatim. A sentence with no such anchor does not belong in this file.

    `## The confidence vocabulary`
    Define exactly three words, in the order the enum declares them: `proven`,
    `inferred`, `unrecoverable`. Give each one the sentence its own doc comment in
    `crates/deform6/src/report.rs` already states. State that these are the only
    three values the JSON report's `confidence` field can hold, and that there is no
    fourth tier and no numeric score. Name the JSON field `confidence` and the
    report keys `items`, `defects` and `limits`, so a reader of the file knows
    where each word appears.

    `## Three facts to read before you run it`
    Three numbered facts, each one paragraph:
    1. The P-code branch is untested. Every one of the corpus programs carries
       `CompilationType` zero, so the tool reports native or P-code from
       `ProjectInfo.lpNativeCode`, and the P-code branch of that field has never
       been run against a real P-code program. Use the literal `lpNativeCode` in
       this paragraph. Say what was run, not what is supported.
    2. `corpus/vb6-code/Hidden-Markov-model/frmHMM.frx` is damaged upstream and is
       excluded by name. Reuse the exclusion reason from
       `crates/deform6/tests/support/frm.rs` verbatim. Use the literal `frmHMM.frx`
       in this paragraph.
    3. Full recompilation was not tested. Reuse the exact sentence
       `crate::report::build_limits` states, beginning "Full recompilation did not
       run.", word for word, so the README and the report agree byte for byte.

    `## Text, encoding and interruption`
    Two paragraphs.
    - Encoding: state that length and equality are measured in encoded bytes,
      never in code points and never in grapheme clusters. Then reuse the code page
      sentence from `build_limits` verbatim, beginning "This run assumes a Western
      code page.".
    - Interruption: state that one run is one process over one file; that the
      library never opens a file and the command line crate owns the file system;
      that `extract` plans every write and refuses the whole run before it writes
      anything when a planned path would leave the output directory, or when a
      symbolic link already sits at a planned path; and that an interrupted run
      leaves a part written directory, because the tool claims no atomicity.

    `## How to run it`
    The two commands, as a reader would type them. Name the exit codes the CLI
    returns if `crates/deform6-cli/src/main.rs` documents them.

    `## The numbers`
    A short section that holds the measured totals. Write no number here yet.
    Instead write five HTML comment markers on their own lines, in this order,
    each with one sentence of surrounding prose naming what the number will be:
    `<!-- measured:corpus-programs -->`, `<!-- measured:procedures -->`,
    `<!-- measured:forms -->`, `<!-- measured:controls -->`,
    `<!-- measured:properties -->`. Plan 06-07 replaces each marker with the number
    the acceptance run measured. A number written in this wave is a number plan
    06-07 might move, and the README is written from the measurement, not before
    it.

    Leave a placeholder heading `## Known limits still open at release` with a
    single sentence saying the section follows. Task 2 fills it.
  </action>
  <verify>
    <automated>test 5 -eq "$(grep -c 'measured:' README.md)"</automated>
    <fails_when>non-zero exit, which happens when the count of deferred number markers is not five, or when README.md does not exist</fails_when>
    <automated>test 0 -eq "$(LC_ALL=C tr -d '\11\12\40-\176' < README.md | wc -c | tr -d ' ')"</automated>
    <fails_when>non-zero exit, which happens when README.md holds any byte outside tab, newline and printable ASCII, for example an em dash or an emoji</fails_when>
    <automated>grep -q 'lpNativeCode' README.md &amp;&amp; grep -q 'frmHMM.frx' README.md &amp;&amp; grep -q 'needs the Visual Basic 6 IDE on' README.md</automated>
    <fails_when>non-zero exit, which happens when any one of the three required facts is absent from README.md</fails_when>
    <automated>test 3 -eq "$(grep -oE '(proven|inferred|unrecoverable)' README.md | sort -u | wc -l | tr -d ' ')"</automated>
    <fails_when>non-zero exit, which happens when fewer than three of the confidence words appear in README.md, so one of the three was dropped</fails_when>
    <automated>grep -q 'items' README.md &amp;&amp; grep -q 'defects' README.md &amp;&amp; grep -q 'limits' README.md &amp;&amp; grep -q 'inspect' README.md &amp;&amp; grep -q 'extract' README.md</automated>
    <fails_when>non-zero exit, which happens when any of the three report keys or either subcommand name is absent from README.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at the repository root and is tracked by git.
    - `README.md` holds no byte outside tab, newline and printable ASCII.
    - `README.md` holds exactly five occurrences of the string `measured:`, one per
      deferred number.
    - `README.md` contains the literal `lpNativeCode`.
    - `README.md` contains the literal `frmHMM.frx`.
    - `README.md` contains the literal `needs the Visual Basic 6 IDE on`, proving
      the recompilation sentence was reused verbatim rather than paraphrased.
    - `README.md` contains the literal `This run assumes a Western code page.`
    - `README.md` names exactly three confidence words: `proven`, `inferred`,
      `unrecoverable`. It names no fourth word and no numeric score.
    - `README.md` names the three JSON report keys `items`, `defects` and `limits`.
    - `README.md` names both subcommands `inspect` and `extract` and no third one.
    - `README.md` contains the heading `## Known limits still open at release`.
  </acceptance_criteria>
  <done>
    A reader of README.md learns what the tool returns, what it does not, the three
    confidence words, and the three facts, and meets no number that the acceptance
    run has not yet measured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Every limit that is still open at release, with the default the tool chose</name>
  <files>README.md</files>
  <read_first>
    - `.planning/research/STRUCTURES.md` section 11, the gap register. It holds
      nineteen rows. Rows 1, 11 and 19 say CLOSED. Row 14 says PARTIALLY CLOSED and
      is still open. Sixteen rows are still open, measured at plan time with
      `awk '/^## 11\. Gap register/,/^## 12\./' .planning/research/STRUCTURES.md | awk -F'|' '/^\| *[0-9]+ *\|/ { if ($5 !~ /^ *CLOSED/) n++ } END { print n+0 }'`
      which prints 16.
    - `.planning/research/FILE-FORMATS.md` section 9. It holds ten numbered items.
      Items 1, 2, 3 and 4 already state a safe default in their own text. Items 5 to
      10 do not, so the default the tool chose must be read out of the code.
    - `README.md` (the file task 1 wrote)
    - `crates/deform6/src/report.rs` `build_limits` (the limit lines the report
      already emits verbatim; reuse one where it answers a row)
    - `.planning/REQUIREMENTS.md` entries for FRM-01, FRM-02, FRM-03 and WRT-03,
      which each carry the paragraph that records why the requirement stays open
      on purpose
  </read_first>
  <action>
    Replace the placeholder section `## Known limits still open at release` in
    `README.md` with two tables.

    The first table has the heading `### From the structure survey` and one row per
    still-open row of `STRUCTURES.md` section 11. Key each row with the register's
    own row number, zero padded, in a first column named `Limit`: `S-02`, `S-03`,
    `S-04`, `S-05`, `S-06`, `S-07`, `S-08`, `S-09`, `S-10`, `S-12`, `S-13`, `S-14`,
    `S-15`, `S-16`, `S-17`, `S-18`. That is sixteen rows. `S-01`, `S-11` and `S-19`
    are absent because those register rows are closed. Columns: `Limit`, `What is
    not known`, `What the tool does instead`.

    The second table has the heading `### From the file format survey` and one row
    per item of `FILE-FORMATS.md` section 9, keyed `F-01` through `F-10`. That is
    ten rows. Same three columns.

    Every row starts each data cell at the line start with a pipe, so the row is
    countable: a row of the first table matches the pattern `^| S-` and a row of
    the second matches `^| F-`.

    For the `What the tool does instead` cell, state the default the tool actually
    chose, in one sentence, in the present tense. Do not restate the register's
    "Resolution path" column, which names how the gap could be closed later, not
    what the code does today. Find the default this way:
    - Where `crate::report::build_limits` already emits a line that answers the
      row, reuse that line verbatim. S-13 and S-15 are answered this way by the
      code page line and the opcode table line.
    - Where the register itself says "Leave opaque", say that the tool reads the
      field and reports nothing from it. S-16 and S-17 are this shape.
    - Where the register says "Do not implement without a sample", say that the
      tool refuses the input and names the byte offset. S-18 is this shape.
    - Otherwise grep `crates/deform6/src/` for the structure or field name the
      register row names, read the code site that chooses the value, and state
      what it chooses. A row whose default you cannot find in the code is not a
      row to guess at: write that the tool reports the fact as `inferred` or
      `unrecoverable` only if the code really does, and otherwise stop and record
      the row as unresolved in the plan summary rather than inventing a default.

    For F-01 through F-04, reuse the safe default each item already states, word
    for word. For F-05 through F-10, read the code the same way as above.

    Open the section with two sentences: one saying that a gap that is still open
    at release is a documented limit and not a silent one, and one naming the two
    surveys the rows come from, so a reader can check the list against its source.

    Add a closing sentence stating the two counts, sixteen and ten, so a later
    reader can check the table against the register without counting by hand.
  </action>
  <verify>
    <automated>test 16 -eq "$(grep -c '^| S-' README.md)"</automated>
    <fails_when>non-zero exit, which happens when the structure survey table does not hold exactly sixteen rows keyed S-NN</fails_when>
    <automated>test 10 -eq "$(grep -c '^| F-' README.md)"</automated>
    <fails_when>non-zero exit, which happens when the file format survey table does not hold exactly ten rows keyed F-NN</fails_when>
    <automated>test "$(awk '/^## 11\. Gap register/,/^## 12\./' .planning/research/STRUCTURES.md | awk -F'|' '/^\| *[0-9]+ *\|/ { if ($5 !~ /^ *CLOSED/) n++ } END { print n+0 }')" -eq "$(grep -c '^| S-' README.md)"</automated>
    <fails_when>non-zero exit, which happens when the count of still-open register rows and the count of README rows disagree; this is the check that catches a row silently dropped or a row closed upstream after this plan ran</fails_when>
    <automated>test "$(awk '/^## 9\. Gaps/,/^## Sources/' .planning/research/FILE-FORMATS.md | grep -cE '^[0-9]+\. \*\*')" -eq "$(grep -c '^| F-' README.md)"</automated>
    <fails_when>non-zero exit, which happens when the count of section 9 items and the count of README rows disagree</fails_when>
    <automated>test 0 -eq "$(LC_ALL=C tr -d '\11\12\40-\176' < README.md | wc -c | tr -d ' ')"</automated>
    <fails_when>non-zero exit, which happens when a limit row introduced a byte outside tab, newline and printable ASCII</fails_when>
    <automated>test 0 -eq "$(grep -c 'S-01\|S-11\|S-19' README.md)"</automated>
    <fails_when>non-zero exit, which happens when a closed register row was listed as an open limit</fails_when>
  </verify>
  <acceptance_criteria>
    - The README holds exactly sixteen rows matching `^| S-`, keyed `S-02`, `S-03`,
      `S-04`, `S-05`, `S-06`, `S-07`, `S-08`, `S-09`, `S-10`, `S-12`, `S-13`,
      `S-14`, `S-15`, `S-16`, `S-17`, `S-18`.
    - The README holds exactly ten rows matching `^| F-`, keyed `F-01` through
      `F-10`.
    - The count of `^| S-` rows equals the count of still-open rows re-derived from
      `STRUCTURES.md` section 11 at check time.
    - The count of `^| F-` rows equals the count of numbered items re-derived from
      `FILE-FORMATS.md` section 9 at check time.
    - No row is keyed `S-01`, `S-11` or `S-19`. Those three register rows are
      closed and are not limits.
    - Every row's third cell is a sentence in the present tense stating what the
      tool does, not a sentence stating how the gap could be closed later.
    - Rows `S-13` and `S-15` reuse a `build_limits` line verbatim. A source
      assertion holds: the README contains the literal `This run assumes a Western
      code page.` and the literal `Opcode table`.
    - `README.md` still holds no byte outside tab, newline and printable ASCII.
  </acceptance_criteria>
  <done>
    Every gap that is still open at release is listed in the README as a limit,
    with the default the tool chose, and the count is checkable against both
    registers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the released README -> the reader | The reader forms an expectation of the tool before they run it. This is the honesty boundary, and it is the only new one Phase 6 opens. |

## STRIDE Threat Register

ASVS level 1. Blocking severity: high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Repudiation | `README.md` capability prose | high | mitigate | Every capability sentence must trace to a `tests/ratios.toml` total, a `Confidence` variant name, or a `limits` string the report emits verbatim. Plan 06-06 adds `scripts/check-claim-surface.sh`, which greps this file and fails on a claim shape, and plan 06-07 runs the honesty audit over it. |
| T-06-02-02 | Repudiation | `README.md` claims about input validation | high | mitigate | Phase 5 delivered the input validation controls. This README states what was run, not what is supported. The P-code paragraph and the recompilation paragraph both name the thing that was never run. Task 1 makes both a counted acceptance criterion. |
| T-06-02-03 | Information disclosure | `README.md` limit rows | medium | accept | The limit rows name byte offsets and structure names from the tool's own surveys. Those surveys are already committed in `.planning/research/` and name no third party secret. Naming a known limit is the documented behaviour this phase exists to deliver. |
| T-06-02-04 | Tampering | `README.md` numbers | medium | mitigate | No corpus-measured number is written in this wave. Five markers defer every number to plan 06-07, which writes them from the acceptance run. This closes the roadmap's own named risk that the README is written from the plan rather than from the measurement. |

Path traversal and injection are canon security topics. They are covered by the
shipped `extract` containment check and by `/gsd-secure-phase`, so they are not
minted as prohibitions here.
</threat_model>

<verification>
- `README.md` exists, is tracked, and holds printable ASCII only.
- The three required facts are present as literals.
- The two limit tables hold sixteen and ten rows, and both counts match a
  re-derivation from the source registers.
- Five deferred number markers remain for plan 06-07.
</verification>

<success_criteria>
- Success criterion 3 is met: the README states the three facts in plain words.
- Success criterion 4 is met: the README lists every still-open gap from
  STRUCTURES section 11 and FILE-FORMATS section 9, with the safe default the tool
  chose for each one.
- The README carries no number the acceptance run has not measured.
</success_criteria>

<output>
Create `.planning/phases/06-version-1-0/06-02-SUMMARY.md` when done. Record any
limit row whose default could not be found in the code, so plan 06-07's honesty
audit can close it rather than inherit a guess.
</output>

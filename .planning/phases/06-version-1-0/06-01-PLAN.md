---
phase: 06-version-1-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/deform6/Cargo.toml
  - crates/deform6/schema/report.schema.json
  - crates/deform6/tests/schema.rs
autonomous: false
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-05, RPT-06]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A committed JSON Schema file describes the report, and it is a second source of truth: it is read from disk, not derived from the Rust types at run time (RPT-01)."
    - "All 44 corpus programs produce a report that validates against the committed schema (RPT-01, RPT-02, RPT-04, RPT-05)."
    - "The schema admits exactly the three lower case confidence words and refuses a fourth (RPT-03)."
    - "A doctored report fails validation, so the schema test can fail and is not a test that only agrees with itself (RPT-02, RPT-03, RPT-05)."
    - "The schema names every defect kind the library can emit, and the count matches the enum in crates/deform6/src/error.rs (RPT-05)."
  prohibitions:
    - statement: "The schema must not be relaxed to make a real report pass. If a report does not validate, the report or the type is wrong, not the schema."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "A passing schema run must not be stated as proof that the schema is complete. It proves only that the 44 reports measured on that run hold no shape the schema refuses."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "crates/deform6/schema/report.schema.json"
      provides: "The committed JSON Schema for the report DeForm6 writes"
      contains: "unrecoverable"
    - path: "crates/deform6/tests/schema.rs"
      provides: "Validates all 44 produced reports against the committed schema, and proves the schema refuses a doctored report"
    - path: "crates/deform6/Cargo.toml"
      provides: "The jsonschema dev-dependency the validation rests on"
      contains: "jsonschema"
  key_links:
    - from: "crates/deform6/tests/schema.rs"
      to: "crates/deform6/schema/report.schema.json"
      via: "reads the schema file from disk under CARGO_MANIFEST_DIR"
      pattern: "schema/report\\.schema\\.json"
    - from: "crates/deform6/tests/schema.rs"
      to: "crates/deform6/src/report.rs"
      via: "deform6::write::project returns the ProjectReport whose to_json output is validated"
      pattern: "write::project"
---

<objective>
Author the JSON Schema for the report DeForm6 writes, and prove every one of the
44 corpus programs produces a report that validates against it.

Purpose: success criterion 1 says all 44 reports validate against the schema. No
schema file exists in this repository today. This is real new work, and decision
D-01 makes it a task of its own rather than a bullet inside a documentation
task. This plan is also the phase tracer: it drives one program through the whole
release-evidence path (read the executable, build the report, serialise it,
validate it against a committed artifact, run it in the gate) before any other
plan expands on that path.

Output: `crates/deform6/schema/report.schema.json`, `crates/deform6/tests/schema.rs`,
and the `jsonschema` dev-dependency that validates against it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-version-1-0/06-RESEARCH.md
@.planning/phases/06-version-1-0/06-PATTERNS.md
</context>

## Artifacts this phase produces

These symbols do not exist in the tree yet. A drift check must not flag them as
unresolved references.

| Artifact | Kind | Plan |
|---|---|---|
| `README.md` | new file | 06-02 |
| `CHANGELOG.md` | new file | 06-05 |
| `LICENSES.md` | new file | 06-05 |
| `crates/deform6/schema/report.schema.json` | new file | 06-01 |
| `crates/deform6/tests/schema.rs` | new file | 06-01 |
| `crates/deform6/tests/gitattributes.rs` | new file | 06-04 |
| `crates/xtask/src/licences.rs` | new file | 06-05 |
| `scripts/check-claim-surface.sh` | new file | 06-06 |
| `scripts/check-release-version.sh` | new file | 06-04 |
| `jsonschema` dev-dependency | new dependency | 06-01 |
| `cargo run -p xtask -- licences` | new xtask subcommand | 06-05 |
| `cargo doc` | new `gate.yml` step | 06-03 |
| `Check the claim surface` | new `gate.yml` step | 06-06 |
| `every_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `the_schema_refuses_a_doctored_report` | new test fn | 06-01 |
| `one_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `every_extension_the_writer_emits_is_named_in_gitattributes` | new test fn | 06-04 |
| `v1.0.0` | new git tag | 06-07 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the jsonschema package is the crate it claims to be</name>
  <files>Cargo.toml, crates/deform6/Cargo.toml</files>
  <what-built>
    Nothing is installed yet. This checkpoint runs before the dependency enters the
    build. Measured at plan time: `cargo search jsonschema` returns
    `jsonschema = "0.56.0"`, and
    `cargo add jsonschema@0.56.0 --dev --no-default-features -p deform6 --dry-run`
    succeeds with every network and TLS feature off.
  </what-built>
  <how-to-verify>
    Open `https://crates.io/crates/jsonschema` and confirm three things:
    1. The repository link points at the `Stranger6667/jsonschema-rs` project.
    2. Version `0.56.0` is the published latest, and the crate has a real download
       history rather than a handful of downloads.
    3. The licence is MIT.
  </how-to-verify>
  <resume-signal>Type "approved", or name a different validator to use instead.</resume-signal>
  <read_first>
    - `.planning/phases/06-version-1-0/06-RESEARCH.md` section "Package Legitimacy Audit"
    - `Cargo.toml` (the `[workspace.dependencies]` table, for the one-line style to follow)
  </read_first>
  <context>
    Phase 6 adds one package-manager dependency: `jsonschema`, as a dev-dependency
    of the `deform6` crate, used only by `crates/deform6/tests/schema.rs`.

    `06-RESEARCH.md`'s Package Legitimacy Audit table covers `cargo-deny` only. It
    does not cover `jsonschema`. Under the package-legitimacy fallback policy an
    uncovered package is treated as `[ASSUMED]`, and an `[ASSUMED]` package needs
    a blocking human check before it is installed. This checkpoint is never
    auto-approvable, whatever `workflow.auto_advance` says.

    Measured at plan time in this worktree:
    - `cargo search jsonschema` returns `jsonschema = "0.56.0"`, described as a
      JSON schema validation library.
    - `cargo add jsonschema@0.56.0 --dev --no-default-features -p deform6 --dry-run`
      succeeds and lists the optional features that stay off:
      `arbitrary-precision`, `conformance`, `idna`, `macros`, `magnus`, `pyo3`,
      `reqwest`, `resolve-async`, `resolve-file`, `resolve-http`, `tls-aws-lc-rs`,
      `tls-ring`.
    - The crate's own documentation states the licence is MIT.

    Default features pull in `reqwest` and a TLS stack. This plan turns them off.
    DeForm6 runs offline over one local file, and a schema validator that can
    open a network connection is not something this project ships at version 1.0.
  </context>
  <verify>
    <human-check>
      Open `https://crates.io/crates/jsonschema` and confirm three things:
      1. The repository link points at the `Stranger6667/jsonschema-rs` project.
      2. Version `0.56.0` is the published latest, and the crate has a real
         download history rather than a handful of downloads.
      3. The licence is MIT.

      Answer: approve, or name a different validator to use instead.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The human records an approve or a reject decision in the plan summary.
    - On approve, the exact dependency line the next task writes is
      `jsonschema = { version = "0.56.0", default-features = false }`.
    - On reject, no task in this plan installs any package, and the plan stops
      for replanning.
  </acceptance_criteria>
  <done>The dependency is approved by a human, or the plan stops.</done>
</task>

<task type="tracer">
  <name>Task 2: One corpus program, end to end, from bytes to a schema-validated report</name>
  <files>Cargo.toml, Cargo.lock, crates/deform6/Cargo.toml, crates/deform6/schema/report.schema.json, crates/deform6/tests/schema.rs</files>
  <read_first>
    - `crates/deform6/src/report.rs` lines 1-110 (the `ProjectReport`, `ReportItem`,
      `Confidence` and `Evidence` definitions, and the `to_json` entry point)
    - `crates/deform6/src/error.rs` lines 20-60 and 380-400 (the `Site` and `Defect`
      definitions; `Defect` carries `site` and `kind` only, never `severity`)
    - `crates/deform6/tests/extract_structural.rs` lines 1-60 and 105-180 (the
      lint-allow header, the module doc-comment register, the `#[path = "support/mod.rs"]`
      import, the temp-directory helper, and the `deform6::inspect` then
      `deform6::write::project` call pair that produces a `ProjectReport`)
    - `Cargo.toml` (`[workspace.dependencies]`) and `crates/deform6/Cargo.toml`
    - `.planning/phases/06-version-1-0/06-PATTERNS.md` section
      "`crates/deform6/tests/schema.rs` (new test)"
  </read_first>
  <action>
    Add `jsonschema = { version = "0.56.0", default-features = false }` to the
    `[workspace.dependencies]` table in the root `Cargo.toml`, and add a
    `[dev-dependencies]` section to `crates/deform6/Cargo.toml` holding
    `jsonschema.workspace = true`. `crates/deform6/Cargo.toml` has no
    `[dev-dependencies]` section today. Default features stay off, so no HTTP or
    file reference resolver is compiled in.

    Create `crates/deform6/schema/report.schema.json`. Write it against JSON Schema
    draft 2020-12. Give it `$schema` set to
    `https://json-schema.org/draft/2020-12/schema` and `$id` set to the URN
    `urn:deform6:report:1`, so that no validator is ever asked to fetch a network
    address. Describe exactly the shape `crates/deform6/src/report.rs` serialises:

    - The root is an object with `"additionalProperties": false` and
      `"required": ["items", "defects", "limits"]`. There is no fourth top-level
      key. `items` is an array of `#/$defs/reportItem`, `defects` is an array of
      `#/$defs/defect`, `limits` is an array of strings.
    - `$defs/reportItem`: an object, `additionalProperties` false, required
      `path`, `confidence`, `basis`, `evidence`. `path` and `basis` are strings.
      `confidence` is an enum holding exactly `proven`, `inferred` and
      `unrecoverable`, in that order, because `Confidence` carries
      `#[serde(rename_all = "lowercase")]` and has exactly three variants.
      `evidence` is an array of `#/$defs/evidence`.
    - `$defs/evidence`: an object, `additionalProperties` false, required
      `offset`, `structure`, `field`, `note`. `offset` is an integer with
      `minimum` 0 and `maximum` 4294967295, because the Rust field is `u32`.
      `structure` and `field` are strings. `note` is `Option<String>` with no
      skip attribute, so the key is always present and its type is
      `["string", "null"]`.
    - `$defs/defect`: an object, `additionalProperties` false, required `site`
      and `kind`. `Defect` holds those two fields only. It does not serialise a
      severity.
    - `$defs/site`: an object, `additionalProperties` false, required `offset`,
      `rva`, `structure`, `field`. `offset` is an integer bounded as above.
      `rva` is `Option<u32>`, so its type is `["integer", "null"]` with the same
      bounds when it is an integer. `structure` and `field` are strings.
    - `$defs/defectKind`: `DefectKind` is an externally tagged serde enum, so one
      value is an object holding exactly one key whose name is the variant name.
      In this task constrain it with `"type": "object"`, `"minProperties": 1`,
      `"maxProperties": 1`, and a `propertyNames` enum naming all seventeen
      variants: `BadMagic`, `OffsetOverflow`, `PastEndOfFile`, `ImplausibleCount`,
      `UnmappedAddress`, `SectionOverlap`, `NoNulTerminator`, `CountMismatch`,
      `UnreadablePointer`, `EmptyName`, `IndexHighByteSet`, `UnrecoverableString`,
      `ClassNameNoDot`, `GuidLengthUnexpected`, `OcxReservedFieldUnexpected`,
      `BlobLenTooSmall`, `StructureUnreadable`. Task 3 replaces this with a
      per-variant payload description.

    Create `crates/deform6/tests/schema.rs`. Open it with the six-lint
    `#![allow(...)]` header block that `crates/deform6/tests/extract_structural.rs`
    and `crates/deform6/tests/ratios.rs` both carry, byte for byte, including its
    `reason` string. Follow it with a module doc comment that states what this file
    proves and what it does not: it proves that the reports this run produced hold
    no shape the committed schema refuses, and it never claims the schema itself is
    complete or hand-checked beyond that.

    Give the file a helper that reads the schema text from
    `Path::new(env!("CARGO_MANIFEST_DIR")).join("schema/report.schema.json")` and
    compiles it once with `jsonschema::validator_for`. The schema is read from
    disk, never rebuilt from the Rust types, because a schema derived from the
    types at run time would agree with a bug in those types.

    Add one `#[test] fn one_corpus_report_validates_against_the_committed_schema()`.
    It reads `corpus/public-domain/PassGen/PassGen.exe` relative to
    `CARGO_MANIFEST_DIR`, calls `deform6::inspect` in `Mode::Strict`, calls
    `deform6::write::project` with the same mode, takes the returned
    `ProjectReport`, serialises it with `to_json`, parses that text with
    `serde_json::from_str` into a `serde_json::Value`, and validates that value.
    On failure, walk `iter_errors` and panic with every error path and message, not
    only the first, so a red run names every shape that disagreed.

    Use the opcode-table argument `deform6::inspect` takes in the same form
    `extract_structural.rs` uses. Do not invent a second way to build it.
  </action>
  <verify>
    <automated>cargo test -p deform6 --test schema</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed", or the run prints "0 filtered out; 0 measured" with no test name</fails_when>
    <automated>test 3 -eq "$(jq '.required | length' crates/deform6/schema/report.schema.json)"</automated>
    <fails_when>non-zero exit, which happens when the root `required` array does not hold exactly three names, or when jq cannot parse the file</fails_when>
    <automated>cargo fmt --all --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --workspace</automated>
    <fails_when>non-zero exit from any of the three, or clippy printing a line beginning "error:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/deform6/schema/report.schema.json` exists, parses as JSON, and its
      root `required` array holds exactly `items`, `defects`, `limits`.
    - The schema's `confidence` enum holds exactly the three strings `proven`,
      `inferred`, `unrecoverable`.
    - `crates/deform6/schema/report.schema.json` names all seventeen `DefectKind`
      variant names. The count matches the enum: `grep -c` over the
      `propertyNames` enum equals the variant count in
      `crates/deform6/src/error.rs`.
    - `crates/deform6/tests/schema.rs` opens with the identical six-lint
      `#![allow(...)]` header block used by `extract_structural.rs`.
    - `crates/deform6/tests/schema.rs` reads the schema from disk. A source
      assertion holds: the file contains the literal `schema/report.schema.json`.
    - `cargo test -p deform6 --test schema` passes and reports one test.
    - `crates/deform6/Cargo.toml` holds a `[dev-dependencies]` section with
      `jsonschema.workspace = true`, and the root `Cargo.toml` holds
      `default-features = false` on that dependency.
  </acceptance_criteria>
  <done>
    One corpus program is read, written, reported and validated against a
    committed schema file, and the three gate commands pass on the tree that
    leaves behind.
  </done>
</task>

<task type="auto">
  <name>Task 3: All 44 programs, and a schema that refuses a doctored report</name>
  <files>crates/deform6/schema/report.schema.json, crates/deform6/tests/schema.rs</files>
  <read_first>
    - `crates/deform6/tests/schema.rs` (the file task 2 wrote)
    - `crates/deform6/tests/corpus_sweep.rs` lines 30-80 (the `corpus_root` and
      `executables` walk that gives all 44 executables, sorted, and the count
      assertion that catches a corpus that grew or shrank)
    - `crates/deform6/src/error.rs` lines 49-290 (every `DefectKind` variant and
      the exact field names of each payload)
    - `crates/deform6/schema/report.schema.json` (the file task 2 wrote)
  </read_first>
  <action>
    Extend `crates/deform6/schema/report.schema.json`. Replace the
    `propertyNames`-only `$defs/defectKind` with a `oneOf` over seventeen
    single-key object schemas, one per `DefectKind` variant. Each branch names
    exactly one property, sets `"additionalProperties": false` at both levels, and
    describes that variant's own payload field names and types as
    `crates/deform6/src/error.rs` declares them. A `u32` field is an integer with
    `minimum` 0 and `maximum` 4294967295. A `&'static str` field is a string. Keep
    the seventeen names in the same order as the enum, so a reader can diff the two
    by eye.

    Extend `crates/deform6/tests/schema.rs` with two tests.

    `#[test] fn every_corpus_report_validates_against_the_committed_schema()` walks
    `corpus/` for every file whose extension compares equal to `exe` without regard
    to case, sorts the result, asserts the count is 44 with a message naming the
    count it found, and for each executable repeats the read, write, serialise and
    validate sequence task 2 established. Compile the schema once, outside the
    loop. On a failure name the corpus key, the JSON pointer of the failing
    instance, and the validator's message. Copy the walk from
    `crates/deform6/tests/corpus_sweep.rs` rather than inventing a second one; that
    file's own doc comment explains why the compare is case-insensitive and why the
    count assertion is what actually catches a corpus that changed.

    `#[test] fn the_schema_refuses_a_doctored_report()` proves the schema has
    teeth. Build one real report, serialise it, parse it to a mutable
    `serde_json::Value`, and then make four separate doctored copies, validating
    each one and asserting that each is rejected:
    1. A copy whose first item's `confidence` is set to a fourth word the enum
       does not hold.
    2. A copy with the `limits` key removed.
    3. A copy carrying an extra top-level key the root schema does not allow.
    4. A copy whose first defect's `kind` object is renamed to a variant name that
       is not in the enum. Skip this case with an explicit assertion message if
       the chosen program produced no defect, and pick a program that does produce
       one instead of silently passing.
    Each assertion states which doctored shape was expected to be refused. A test
    that cannot fail is worse than no test, and this test is the proof that the
    other one can.
  </action>
  <verify>
    <automated>cargo test -p deform6 --test schema</automated>
    <fails_when>non-zero exit, or the summary line does not read "3 passed"</fails_when>
    <automated>cargo test -p deform6 --test schema -- the_schema_refuses_a_doctored_report</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed" because the filter matched no test</fails_when>
    <automated>test 17 -eq "$(jq '.["$defs"].defectKind.oneOf | length' crates/deform6/schema/report.schema.json)"</automated>
    <fails_when>non-zero exit, which happens when the `oneOf` array does not hold exactly seventeen branches, or when jq prints `null` because the key path is absent</fails_when>
    <automated>cargo fmt --all --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --workspace</automated>
    <fails_when>non-zero exit from any of the three, or clippy printing a line beginning "error:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo test -p deform6 --test schema` reports exactly three tests, all
      passing.
    - The 44-program test asserts the corpus count is 44 and names the count it
      found when the assertion fails.
    - `$defs/defectKind` holds a `oneOf` with exactly seventeen branches, matching
      the seventeen variants in `crates/deform6/src/error.rs`.
    - The doctored-report test makes four separate assertions, one per doctored
      shape, and each assertion message names the shape it expected to be refused.
    - No test in this file derives the schema from the Rust types at run time. A
      source assertion holds: the file names the schema path and calls a validator
      compiled from that text.
  </acceptance_criteria>
  <done>
    All 44 corpus programs produce a report that validates against the committed
    schema, and a doctored report is proved to fail.
  </done>
</task>

</tasks>

## Flagged assumptions from the edge probe

Two `unclassified` probe rows belong to this plan's requirements. The probe could
not classify them, so they are not auto-resolved. They are carried forward
visibly rather than dropped.

| Requirement | Probe row | Why it stays unresolved |
|---|---|---|
| RPT-01 | `unclassified` — review manually | The probe raised no shaped edge for "the tool writes one JSON report beside the project". The schema work in this plan constrains the report's content, not where the file is placed or what happens when the output directory already holds one. That path is owned by the CLI and is re-verified in plan 06-07. |
| RPT-06 | `unclassified` — review manually | The probe raised no shaped edge for "an uncertain region of recovered code carries an apostrophe comment". The comment text is written by `crates/deform6/src/write/comment.rs` and does not reach the JSON report, so this schema cannot check it. It stays a manual review item for plan 06-07's honesty audit. |

## Edge-probe ledger

The full arithmetic for all 107 probe rows lives in `06-07-PLAN.md`, under
"Edge-probe ledger". This plan contributes the two flagged assumptions above.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io -> the build | A new dev-dependency enters the build for the first time at version 1.0 |
| an untrusted `.exe` -> the library reader | The test drives real corpus bytes through `deform6::inspect` |
| the library -> the temporary output directory | `deform6::write::project` returns file contents; this test does not write them to disk |

## STRIDE Threat Register

ASVS level 1. Blocking severity: high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-SC | Tampering | `cargo add jsonschema` | high | mitigate | Task 1 is a blocking human checkpoint that confirms the package identity, repository and licence on crates.io before any install. The package is uncovered by `06-RESEARCH.md`'s audit table, so the `[ASSUMED]` fallback applies. |
| T-06-01-01 | Denial of Service | `jsonschema` default features | high | mitigate | `default-features = false` keeps `reqwest`, `resolve-http`, `resolve-async` and both TLS stacks out of the build. A validator that can open a network connection is not shipped at version 1.0. The schema uses a `urn:` `$id` so no resolver is ever asked for an address. |
| T-06-01-02 | Tampering | `deform6::inspect` over corpus bytes | medium | accept | The bounded `Region` reader, `checked_add` on every file-derived offset, `#![forbid(unsafe_code)]` and the clippy deny wall are already shipped and are re-verified by the full gate. This plan adds no new read path. |
| T-06-01-03 | Information disclosure | the report text this test builds | low | accept | The test holds the report in memory and never writes it to disk, so no path traversal surface is reached. `deform6::write::project` returns file contents rather than writing them. |
</threat_model>

<verification>
- `cargo test -p deform6 --test schema` reports three passing tests.
- `cargo fmt --all --check`, `cargo clippy --all-targets -- -D warnings` and
  `cargo test --workspace` all pass.
- `crates/deform6/schema/report.schema.json` is tracked by git and holds the
  three top-level keys and the seventeen defect kinds.
</verification>

<success_criteria>
- All 44 corpus programs produce a report that validates against the committed
  schema, measured by a test, not asserted.
- The schema is proved able to refuse: four doctored shapes each fail validation.
- Decision D-01 is delivered: the schema is authored this phase, from the current
  `ProjectReport`, `ReportItem`, `Confidence`, `Evidence` and `Defect` types.
</success_criteria>

<output>
Create `.planning/phases/06-version-1-0/06-01-SUMMARY.md` when done. Record the
number of defect kinds the corpus actually produced, so plan 06-07 can state a
measured number rather than a derived one.
</output>

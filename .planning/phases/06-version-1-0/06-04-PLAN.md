---
phase: 06-version-1-0
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - scripts/check-release-version.sh
  - crates/deform6/tests/gitattributes.rs
autonomous: false
requirements: [FRM-05, WRT-01, WRT-05, WRT-06]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The workspace version is 1.0.0, and every crate inherits it through `version.workspace = true`."
    - "A script measures whether the git tag on HEAD matches the version in Cargo.toml, rather than a human asserting that it does."
    - "The version check is proved able to fail: it reports a mismatch against a deliberately wrong tag before it is trusted to report a match."
    - "Every file extension the writer emits is named in `.gitattributes`, measured from the writer's own output rather than from a list somebody typed (WRT-01, WRT-05, WRT-06)."
    - "`*.frx` and `*.ctx` are both marked binary in `.gitattributes`, and a test says so (FRM-05, VER-06)."
  prohibitions:
    - statement: "A version number must not be used to imply a stability guarantee that the test suite does not support. What 1.0.0 promises is the public library API shape, and nothing more."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "`.gitattributes` must not be rewritten to make the audit pass. The file already carries a reasoned rule set. If the audit fails, the missing extension is added, and no existing rule is weakened."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "scripts/check-release-version.sh"
      provides: "Measures the git tag on HEAD against the version in Cargo.toml, and proves it can fail"
    - path: "crates/deform6/tests/gitattributes.rs"
      provides: "Audits .gitattributes against the extension set the writer actually emits"
    - path: "Cargo.toml"
      provides: "The workspace version, 1.0.0"
      contains: "1.0.0"
  key_links:
    - from: "scripts/check-release-version.sh"
      to: "Cargo.toml"
      via: "reads the single line-anchored version key of the [workspace.package] table"
      pattern: "\\^version = "
    - from: "crates/deform6/tests/gitattributes.rs"
      to: ".gitattributes"
      via: "reads the committed attribute file and matches it against the writer's own output extensions"
      pattern: "\\.gitattributes"
---

<objective>
Move the workspace version to 1.0.0, add a script that measures the tag against
it, and audit `.gitattributes` against the extension set the writer emits.

Purpose: success criterion 5 requires the tag to match the version in
`Cargo.toml`. `Cargo.toml` pins `version = "0.1.0"` workspace-wide today, and the
only tag in the repository is `backup-pre-footer-rewrite`, which is unrelated to
a release. Decision D-02 settles the target: the version becomes 1.0.0 and the
tag is `v1.0.0`. `06-RESEARCH.md` found that `.gitattributes` for `*.frx` and
`*.ctx` already exists and is correct, so this plan audits it rather than
authoring it.

Output: `Cargo.toml` at 1.0.0, `scripts/check-release-version.sh`, and
`crates/deform6/tests/gitattributes.rs`. Plan 06-07 creates the tag itself, after
the acceptance run is green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@AGENTS.md
@.planning/phases/06-version-1-0/06-RESEARCH.md
@.planning/phases/06-version-1-0/06-PATTERNS.md
</context>

## Artifacts this phase produces

These symbols do not exist in the tree yet. A drift check must not flag them as
unresolved references.

| Artifact | Kind | Plan |
|---|---|---|
| `README.md` | new file | 06-02 |
| `CHANGELOG.md` | new file | 06-05 |
| `LICENSES.md` | new file | 06-05 |
| `crates/deform6/schema/report.schema.json` | new file | 06-01 |
| `crates/deform6/tests/schema.rs` | new file | 06-01 |
| `crates/deform6/tests/gitattributes.rs` | new file | 06-04 |
| `crates/xtask/src/licences.rs` | new file | 06-05 |
| `scripts/check-claim-surface.sh` | new file | 06-06 |
| `scripts/check-release-version.sh` | new file | 06-04 |
| `jsonschema` dev-dependency | new dependency | 06-01 |
| `cargo run -p xtask -- licences` | new xtask subcommand | 06-05 |
| `cargo doc` | new `gate.yml` step | 06-03 |
| `Check the claim surface` | new `gate.yml` step | 06-06 |
| `every_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `the_schema_refuses_a_doctored_report` | new test fn | 06-01 |
| `one_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `every_extension_the_writer_emits_is_named_in_gitattributes` | new test fn | 06-04 |
| `v1.0.0` | new git tag | 06-07 |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the move to 1.0.0 and the tag name</name>
  <files>Cargo.toml</files>
  <decision>
    Move the workspace version from `0.1.0` to `1.0.0`, and name the release tag
    `v1.0.0`.
  </decision>
  <context>
    This is a one-way door. Three things become hard to undo once 1.0.0 ships:

    1. **The semantic version promise.** After 1.0.0, a breaking change to the
       public library API of the `deform6` crate needs 2.0.0. Under `0.x` a
       breaking change needs only a minor bump. The public surface this promise
       covers is real: `deform6::inspect`, `deform6::write::project`,
       `ProjectReport`, `ReportItem`, `Confidence`, `Evidence`, `Defect`, `Site`,
       `DefectKind`, `Severity` and `SafeName` are all public today.
    2. **The report shape.** Plan 06-01 committed
       `crates/deform6/schema/report.schema.json`. A consumer who validates against
       that schema now has a contract. Changing the report's top level keys after
       1.0.0 breaks that consumer.
    3. **The tag.** A tag that is pushed to a remote can be deleted but cannot be
       recalled from anyone who already fetched it. This plan creates no tag. Plan
       06-07 creates it locally. Nothing is pushed unless the human asks.

    Decision D-02 already records the human's choice. This checkpoint does not
    re-ask it. It confirms the exact strings before the agent writes them, and it
    records the promise in the plan so a later phase reads it rather than
    rediscovers it.

    The alternative, kept for the record: stay on `0.x` and name the tag
    `v0.9.0`. That keeps the API free to move without a major bump, and it makes
    the milestone name and the package version disagree. D-02 rejects it.
  </context>
  <options>
    <option id="one-zero-zero">
      <name>version = "1.0.0" and the tag v1.0.0</name>
      <pros>This is decision D-02. The package version and the milestone name agree. A consumer reading the version learns that the public library API and the report schema are now a contract.</pros>
      <cons>A breaking change to the public library API after this release needs 2.0.0, not a minor bump.</cons>
    </option>
    <option id="zero-nine-zero">
      <name>Stay on 0.x and name the tag v0.9.0</name>
      <pros>The public API stays free to move without a major bump.</pros>
      <cons>The milestone name and the package version disagree, and success criterion 5 then measures a tag against a version that does not say 1.0. D-02 rejects this.</cons>
    </option>
  </options>
  <resume-signal>Type "approved" for option one-zero-zero, or name the exact version string and tag string to use instead.</resume-signal>
  <verify>
    <human-check>
      Confirm the two strings: the `Cargo.toml` version is `1.0.0`, and the tag is
      `v1.0.0`. Confirm that after this release a breaking change to the public
      library API needs 2.0.0.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The two exact strings are recorded in the plan summary: the version string
      and the tag string.
    - The record states that a breaking change to the public library API after
      this release needs a major bump.
  </acceptance_criteria>
  <done>The version string and the tag string are confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: The version, and a check that measures the tag against it</name>
  <files>Cargo.toml, Cargo.lock, scripts/check-release-version.sh</files>
  <reversibility rating="one-way">Version 1.0.0 commits this project to a semantic version promise over a public library API that other code can now depend on; undoing it means either a 2.0.0 major bump or a published contract that moved under a consumer.</reversibility>
  <read_first>
    - `Cargo.toml`. The `[workspace.package]` table holds the single
      `version = "0.1.0"` key. Measured at plan time,
      `grep -c '^version = ' Cargo.toml` prints 1, so a line-anchored match finds
      the workspace version and never a dependency's version field.
    - `crates/deform6/Cargo.toml`, `crates/deform6-cli/Cargo.toml` and
      `crates/xtask/Cargo.toml`. All three already say `version.workspace = true`,
      measured at plan time, so the workspace bump is a one-line change and no
      crate manifest needs editing.
    - `scripts/prove-lint-wall.sh` in full. It is the canonical shape every script
      in `scripts/` shares: the header comment block naming what the script proves
      and why the three gate commands cannot, `set -eu`, the
      `ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)` line followed by
      `cd "$ROOT"`, a `trap cleanup EXIT`, a probe that proves the check fires, and
      a clear PASS or FAIL message that names what was found.
    - `crates/deform6-cli/src/main.rs`, for how `clap` renders the version in
      `--version`. Measured at plan time, `cargo run -q -p deform6-cli -- -V`
      prints `deform6 0.1.0`.
  </read_first>
  <action>
    Change the single `version` key in the `[workspace.package]` table of the root
    `Cargo.toml` from `0.1.0` to `1.0.0`. Change nothing else in that file. Run a
    build so `Cargo.lock` records the new workspace member versions, and commit the
    lock file with it.

    Create `scripts/check-release-version.sh`, following the shape of
    `scripts/prove-lint-wall.sh`. Make it executable.

    Header comment block: state that the three gate commands never look at a git
    tag, so a tag that names a version the manifest does not hold passes the gate
    silently, and this script is the only guard on success criterion 5's last
    clause. State how to run it, both with an explicit tag argument and with none.

    Body:
    - Read the version with `awk -F'"' '/^version = /{print $2; exit}' Cargo.toml`.
      Fail with a named message if the result is empty.
    - Take the tag from the first argument. When no argument is given, read it from
      `git describe --tags --exact-match` and treat a non-zero exit as "HEAD carries
      no tag". Fail with a message naming that, rather than passing silently: a
      check that passes because it found nothing to check is worse than no check.
    - Compare the tag against the version with a `v` prefix. On a mismatch print
      both strings and exit 1. On a match print a line naming both and exit 0.
    - Add a `--self-test` mode. It runs the same comparison against a tag string
      built from the real version with its last component changed, asserts that the
      comparison reports a mismatch, and exits non-zero if the comparison reported
      a match instead. This is the probe: it proves the script can fail before the
      release trusts it to pass. Print a PASS line naming the wrong tag it planted
      and the mismatch it observed.
  </action>
  <verify>
    <automated>test "1.0.0" = "$(awk -F'"' '/^version = /{print $2; exit}' Cargo.toml)"</automated>
    <fails_when>non-zero exit, which happens when the workspace version is not the string 1.0.0</fails_when>
    <automated>test "deform6 1.0.0" = "$(cargo run -q -p deform6-cli -- -V)"</automated>
    <fails_when>non-zero exit, which happens when the binary still reports the old version, meaning a crate did not inherit the workspace version</fails_when>
    <automated>sh scripts/check-release-version.sh --self-test</automated>
    <fails_when>non-zero exit, or the output holds no PASS line naming the planted wrong tag; a silent zero exit with no output means the self test did not run</fails_when>
    <automated>sh scripts/check-release-version.sh v1.0.0</automated>
    <fails_when>non-zero exit, which happens when the version in Cargo.toml is not 1.0.0</fails_when>
    <automated>! sh scripts/check-release-version.sh v9.9.9</automated>
    <fails_when>non-zero exit from the negation, which happens when the script exited zero for a tag that does not match the manifest version; this is the failing direction of the check itself</fails_when>
    <automated>cargo fmt --all --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --workspace</automated>
    <fails_when>non-zero exit from any of the three, or clippy printing a line beginning "error:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml`'s `[workspace.package]` table holds `version = "1.0.0"` and the
      file holds exactly one line-anchored `version = ` key.
    - `cargo run -q -p deform6-cli -- -V` prints exactly `deform6 1.0.0`.
    - `scripts/check-release-version.sh` exists, is executable, and opens with
      `#!/bin/sh` followed by a comment block and `set -eu`.
    - The script exits 0 for the argument `v1.0.0` and exits 1 for `v9.9.9`.
    - The script exits non-zero, with a message naming that HEAD carries no tag,
      when it is run with no argument on a commit that carries no tag.
    - `sh scripts/check-release-version.sh --self-test` passes and prints the
      planted wrong tag it used.
    - No crate manifest was edited. A source assertion holds: all three files under
      `crates/*/Cargo.toml` still say `version.workspace = true`.
  </acceptance_criteria>
  <done>
    The workspace is at 1.0.0, and a script measures the tag against it and is
    proved able to report a mismatch.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit .gitattributes against the extensions the writer emits</name>
  <files>crates/deform6/tests/gitattributes.rs</files>
  <read_first>
    - `.gitattributes` in full. It already marks `*.frx`, `*.ctx`, `*.dsx`, `*.exe`,
      `*.dll` and `*.ocx` as `binary`, and `*.frm`, `*.bas`, `*.cls`, `*.ctl`,
      `*.vbp`, `*.vbw` and `*.mak` as `-text`. Its header comment records the exact
      `frmHMM.frx` corruption that caused the rule. Do not weaken any existing rule.
    - `crates/deform6/tests/extract_structural.rs` lines 1-60 and 105-180, for the
      lint-allow header block, the `deform6::inspect` then `deform6::write::project`
      call pair, and the shape of the `Written` value that call returns, including
      the file name field the test reads.
    - `crates/deform6/src/write/mod.rs`, for the set of file kinds
      `deform6::write::project` can return.
    - `06-RESEARCH.md` Pitfall 3, which states plainly that this file was written in
      an earlier phase and the real work here is an audit, not an authoring job.
  </read_first>
  <action>
    Create `crates/deform6/tests/gitattributes.rs`. Open it with the six-lint
    `#![allow(...)]` header block that `crates/deform6/tests/extract_structural.rs`
    carries, byte for byte. Follow it with a module doc comment stating that this
    file audits the committed `.gitattributes` against the extension set the writer
    actually produces, and that it never rewrites that file.

    Add `#[test] fn every_extension_the_writer_emits_is_named_in_gitattributes()`.
    It reads `corpus/public-domain/PassGen/PassGen.exe` relative to
    `CARGO_MANIFEST_DIR`, calls `deform6::inspect` in `Mode::Strict` and
    `deform6::write::project` with the same mode, and collects the set of lower
    case file extensions from the names of every file that call returned. It then
    reads `.gitattributes` from the repository root and asserts that each collected
    extension appears in that file on a line that also holds `binary` or `-text`.
    On failure, name the extension that has no rule and the file the writer
    produced it for, so a reader knows exactly which line to add.

    Add `#[test] fn frx_and_ctx_are_marked_binary()`. It asserts that
    `.gitattributes` holds a line naming `*.frx` with `binary` and a line naming
    `*.ctx` with `binary`. These two are the deliverable the roadmap names for this
    plan. They are already present, so this test is the record that they are, and
    the guard against a later edit that removes one.

    Build the assertions from the file's shape, not from one form of it: match a
    line that names the pattern and the attribute with whitespace between them in
    any amount, not a single exact string with one space. A helper that looked for
    one space before an equals sign is exactly the trap `AGENTS.md` records.

    Do not edit `.gitattributes` unless the audit fails. If it fails, add the
    missing extension with a comment saying why, and weaken no existing rule.
  </action>
  <verify>
    <automated>cargo test -p deform6 --test gitattributes</automated>
    <fails_when>non-zero exit, or the summary line does not read "2 passed"</fails_when>
    <automated>test 2 -eq "$(grep -cE '^\*\.(frx|ctx)[[:space:]]+binary' .gitattributes)"</automated>
    <fails_when>non-zero exit, which happens when either `*.frx` or `*.ctx` is not marked binary in `.gitattributes`</fails_when>
    <automated>cargo fmt --all --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --workspace</automated>
    <fails_when>non-zero exit from any of the three, or clippy printing a line beginning "error:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo test -p deform6 --test gitattributes` reports exactly two passing
      tests.
    - The extension set the test collects comes from the writer's own output, not
      from a list written into the test. A source assertion holds: the file names
      `deform6::write::project` and does not hold a hard coded array of extension
      strings for the derived set.
    - `.gitattributes` still holds every rule it held before this task. A source
      assertion holds: `git diff main -- .gitattributes` is empty, or holds only
      added lines and no removed line.
    - `*.frx` and `*.ctx` are both matched by a line marking them `binary`.
  </acceptance_criteria>
  <done>
    The `.gitattributes` rules are measured against what the writer actually emits,
    and the two extensions the roadmap names are proved marked binary.
  </done>
</task>

</tasks>

## Flagged assumptions from the edge probe

Three `unclassified` probe rows belong to this plan's requirements.

| Requirement | Probe row | Why it stays unresolved |
|---|---|---|
| FRM-05 | `unclassified` — review manually | The probe raised no shaped edge for "the tool recovers the resource blobs and writes an `.frx`". This plan proves `*.frx` is marked binary so git cannot damage one, which is a storage concern, not a recovery concern. Whether an `.frx` the tool writes is byte-correct is measured by `extract_structural.rs` and is re-verified in plan 06-07. |
| WRT-01 | `unclassified` — review manually | The probe raised no shaped edge for "`deform6 extract` writes a project directory". The output-directory containment check and the symbolic link pre-plant check are shipped and re-verified in plan 06-07. What happens when the output directory already holds a project is governed by `--force`, and is a CLI behaviour this plan does not touch. |
| WRT-06 | `unclassified` — review manually | The probe raised no shaped edge for "the tool writes CRLF line endings and the code page the IDE expects". This plan proves git will not normalise those line endings away. Whether the bytes are right is measured by `extract_structural.rs`. The code page choice is a documented limit, listed in the README by plan 06-02. |

## Edge-probe ledger

The full arithmetic for all 107 probe rows lives in `06-07-PLAN.md`, under
"Edge-probe ledger". This plan contributes the three flagged assumptions above.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a published tag -> everyone who fetches it | A tag cannot be recalled from a fetcher. This plan creates none. |
| git's line ending normalisation -> the committed corpus bytes | The upstream `frmHMM.frx` damage is the recorded proof that this boundary is real |
| the version string -> a downstream consumer of the library | After 1.0.0 the public API shape is a promise |

## STRIDE Threat Register

ASVS level 1. Blocking severity: high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Repudiation | the release tag | high | mitigate | This plan creates no tag and pushes nothing. Plan 06-07 creates the tag locally, after the acceptance run is green, and pushes nothing unless the human asks. Task 1 is a blocking decision checkpoint on the one-way door. |
| T-06-04-02 | Tampering | git line ending normalisation over `*.frx` | high | mitigate | Task 3 asserts `*.frx` and `*.ctx` are marked binary, and audits every extension the writer emits against the committed rules. `corpus/vb6-code/Hidden-Markov-model/frmHMM.frx` is the recorded case where this exact boundary already cost a correct byte. |
| T-06-04-03 | Spoofing | a tag that names a version the manifest does not hold | high | mitigate | `scripts/check-release-version.sh` measures the two against each other, refuses when HEAD carries no tag rather than passing silently, and carries a self test that plants a wrong tag and requires a mismatch. |
| T-06-04-04 | Tampering | `Cargo.lock` after the version bump | medium | accept | The bump changes only the three workspace member versions in the lock file. The dependency set is unchanged, and plan 06-05 re-derives the licence table from `cargo metadata` after this plan lands. |
</threat_model>

<verification>
- `Cargo.toml` holds `version = "1.0.0"` and the binary reports `deform6 1.0.0`.
- `sh scripts/check-release-version.sh --self-test` passes.
- `sh scripts/check-release-version.sh v9.9.9` exits 1.
- `cargo test -p deform6 --test gitattributes` reports two passing tests.
- The three gate commands pass.
</verification>

<success_criteria>
- Decision D-02 is delivered: the version is 1.0.0, and the tag name `v1.0.0` is
  confirmed and recorded.
- Success criterion 5's tag clause has a check that measures rather than asserts,
  and that check is proved able to fail.
- The `.gitattributes` deliverable the roadmap names is audited, not re-authored.
</success_criteria>

<output>
Create `.planning/phases/06-version-1-0/06-04-SUMMARY.md` when done. Record the
exact version string and tag string the checkpoint confirmed, so plan 06-07 uses
them and does not re-derive them.
</output>

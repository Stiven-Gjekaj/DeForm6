---
phase: 06-version-1-0
plan: 07
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06"]
files_modified:
  - README.md
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-06, FRM-06, SAF-01, SAF-04, SAF-05, VER-01, VER-02, VER-03, VER-04, VER-05, WRT-02, WRT-04, WRT-07]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`deform6 extract` runs to completion on all 44 corpus programs, the structural check passes for all 44, the JSON report validates against the schema for all 44, and every pinned ratio in tests/ratios.toml holds unchanged (VER-01, VER-02, VER-03, VER-05, WRT-02, WRT-04, WRT-07)."
    - "Every number in README.md is the number a test reported on this run, not a number calculated from a part and not a number copied out of a planning document."
    - "An empty file and a one byte file refuse as not a portable executable in both strict and salvage mode, and the no-panic proof replays every input this repository can reach through both modes and the writer without ending the process. This answers the `empty` probe row for all 26 requirements that raised one: DET-03, DET-04, DET-06, FRM-01, FRM-02, FRM-03, FRM-04, FRM-06, OBJ-01, OBJ-03, OBJ-04, OBJ-06, RPT-02, RPT-03, RPT-04, RPT-05, SAF-02, SAF-04, SAF-05, VER-01, VER-02, VER-03, VER-05, VER-06, WRT-02, WRT-03."
    - "Two recovered names that clamp to the same forty bytes are issued distinct names, a thousand controls sharing one raw name each get a distinct name, and two objects whose raw names collide name two different files. This answers the `adjacency` probe row for all 16 requirements that raised one: DET-04, FRM-01, FRM-02, FRM-03, FRM-06, OBJ-01, OBJ-03, OBJ-04, RPT-02, RPT-03, RPT-04, RPT-05, SAF-05, VER-01, VER-02, WRT-02."
    - "Output order is specified and stable: no hash keyed container may enter crates/deform6/src, the ordered output wall proves it on every push, properties are written in alphabetical order within a block, and two salvage runs over the same bytes give byte identical JSON. This answers the `ordering` probe row for all 16 requirements that raised one: DET-04, FRM-01, FRM-02, FRM-03, FRM-06, OBJ-01, OBJ-03, OBJ-04, RPT-02, RPT-03, RPT-04, RPT-05, SAF-05, VER-01, VER-02, WRT-02."
    - "No allocation is sized from a length field in the file before that length is checked against the real size of the file: a 4096 byte image declaring a form count of 0xFFFF allocates nothing and yields exactly one ImplausibleCount defect. This answers the `boundary` probe row for all 4 requirements that raised one: FRM-02, FRM-03, FRM-06, RPT-03."
    - "The pinned ratio is recovered divided by declared, rounded to two decimal places, and recomputed from the two counts on every run, so a hand edited ratio that disagrees with its own counts fails. This answers the `precision` probe row for all 4 requirements that raised one: FRM-02, FRM-03, FRM-06, RPT-03."
    - "The release tag is created locally and nothing is pushed to any remote."
  prohibitions:
    - statement: "Nothing is pushed to a remote and no tag is published unless the human asks. A tag can be deleted but cannot be recalled from anyone who already fetched it."
      category: values
      status: resolved
      verification: judgment
    - statement: "A number written into README.md must not be calculated from a part. Give the number the test reported, or give none."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "A result measured on a synthetic fixture must not be presented as a result measured on the corpus."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "README.md"
      provides: "The released claim surface, with every number filled from the acceptance run"
      contains: "44"
  key_links:
    - from: "README.md"
      to: "tests/ratios.toml"
      via: "every filled number is the number the pinned ratio gate test asserts"
      pattern: "44"
    - from: "scripts/check-release-version.sh"
      to: "the v1.0.0 tag"
      via: "run with no argument, it reads the tag from git describe and compares it against Cargo.toml"
      pattern: "check-release-version"
---

<!-- planner-discipline-allow: measured: -->

<objective>
Run the acceptance measurement over all 44 corpus programs, fill the README's
deferred numbers from it, audit every released claim against a measured result,
and create the `v1.0.0` tag.

Purpose: success criterion 1 and the last clause of success criterion 5. The
roadmap's own named risk is that the README is the last place a claim can drift,
and that it must be written after the measurement, from the measurement, and not
from the plan. Plan 06-02 left five markers for exactly this reason. This plan
replaces them with numbers a test reported.

Output: `README.md` with every number filled, a green full gate, and the local
`v1.0.0` tag.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@.planning/phases/06-version-1-0/06-RESEARCH.md
@.planning/phases/06-version-1-0/06-VALIDATION.md
</context>

## Artifacts this phase produces

These symbols do not exist in the tree yet. A drift check must not flag them as
unresolved references.

| Artifact | Kind | Plan |
|---|---|---|
| `README.md` | new file | 06-02 |
| `CHANGELOG.md` | new file | 06-05 |
| `LICENSES.md` | new file | 06-05 |
| `crates/deform6/schema/report.schema.json` | new file | 06-01 |
| `crates/deform6/tests/schema.rs` | new file | 06-01 |
| `crates/deform6/tests/gitattributes.rs` | new file | 06-04 |
| `crates/xtask/src/licences.rs` | new file | 06-05 |
| `scripts/check-claim-surface.sh` | new file | 06-06 |
| `scripts/check-release-version.sh` | new file | 06-04 |
| `jsonschema` dev-dependency | new dependency | 06-01 |
| `cargo run -p xtask -- licences` | new xtask subcommand | 06-05 |
| `cargo doc` | new `gate.yml` step | 06-03 |
| `Check the claim surface` | new `gate.yml` step | 06-06 |
| `every_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `the_schema_refuses_a_doctored_report` | new test fn | 06-01 |
| `one_corpus_report_validates_against_the_committed_schema` | new test fn | 06-01 |
| `every_extension_the_writer_emits_is_named_in_gitattributes` | new test fn | 06-04 |
| `v1.0.0` | new git tag | 06-07 |

<tasks>

<task type="auto">
  <name>Task 1: The acceptance run over all 44, and the numbers it reported</name>
  <files>README.md</files>
  <read_first>
    - `README.md` (written by plan 06-02). It holds five markers, each a HTML
      comment naming a number that this task fills.
    - `tests/ratios.toml`, the header comment block in full. It states the totals
      the pinned gate asserts and why object recovery is deliberately not pinned
      there.
    - `crates/deform6/tests/ratios.rs`, the test function names. Three of them name
      the exact totals in words:
      `the_pinned_file_holds_forty_four_entries_and_the_totals_one_hundred_eighty_five_and_nine_hundred_four`,
      `the_pinned_file_holds_fifty_two_of_fifty_three_forms_and_six_hundred_eighty_six_of_six_hundred_eighty_six_controls`,
      and
      `the_pinned_file_holds_eight_hundred_seven_property_records_and_one_hundred_thirty_six_written_lines`.
      Read the assertions, not only the names.
    - `crates/deform6/tests/extract_structural.rs`, `crates/deform6/tests/corpus_sweep.rs`,
      `crates/deform6/tests/differential.rs`, `crates/deform6/tests/schema.rs`.
    - `06-VALIDATION.md`, the criterion to check map.
  </read_first>
  <action>
    Run the five checks success criterion 1 names, one at a time, and record each
    result in the plan summary with the count of tests that passed:

    - `cargo test -p deform6 --test corpus_sweep`, which reads all 44 corpus
      executables and reports what they hold (DET-01, DET-02, DET-03, DET-06).
    - `cargo test -p deform6 --test extract_structural`, which drives the real
      write path over all 44 and reads every file back through the second,
      independent reader (WRT-02, WRT-04, WRT-07, FRM-06).
    - `cargo test -p deform6 --test schema`, which validates all 44 produced reports
      against the committed schema.
    - `cargo test -p deform6 --test ratios`, which proves every pinned ratio in
      `tests/ratios.toml` holds unchanged (VER-01, VER-02, VER-03, VER-05).
    - `cargo test -p deform6 --test differential`, which compares each recovered
      object and procedure against the original source in both directions.

    Then replace the five markers in `README.md` with the numbers those runs
    reported. Take each number from the assertion the pinned ratio gate makes, not
    from `.planning/REQUIREMENTS.md`, not from `06-RESEARCH.md`, and not from this
    plan. Those documents were written at different times and one of them may have
    moved.

    - `corpus-programs`: the count of corpus executables the sweep asserted.
    - `procedures`: the recovered and declared procedure totals, written as two
      counts, never as one derived fraction.
    - `forms`: the recovered and declared form totals, as two counts.
    - `controls`: the recovered and declared control totals, as two counts.
    - `properties`: the property record total and the written line total, as two
      counts.

    Write each number as the pair of counts. Do not write a single derived figure
    beside it. `AGENTS.md` states the rule: give the number you can prove, and do
    not give a number you calculated from a part.

    Add one short sentence to the README's numbers section naming where each number
    comes from: `tests/ratios.toml` and the gate test that asserts it. A reader who
    wants to check a number must be able to find the test that produced it.

    Add one sentence stating that 52 of 53 forms means one corpus form refuses, that
    the refusal names the byte offset and the byte the code expected there, and that
    a refusal is the correct result because the tool does not print a tree it cannot
    prove. Do not let the pair of counts stand without that sentence: a bare "52 of
    53" reads like a failure rather than a designed refusal.
  </action>
  <verify>
    <automated>cargo test -p deform6 --test corpus_sweep</automated>
    <fails_when>non-zero exit, or the summary line does not read "2 passed"</fails_when>
    <automated>cargo test -p deform6 --test extract_structural</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed"</fails_when>
    <automated>cargo test -p deform6 --test schema</automated>
    <fails_when>non-zero exit, or the summary line does not read "3 passed"</fails_when>
    <automated>cargo test -p deform6 --test ratios</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed"; a moved ratio prints the new number and the paste block for tests/ratios.toml</fails_when>
    <automated>cargo test -p deform6 --test differential</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed"</fails_when>
    <automated>test 0 -eq "$(grep -c 'measured:' README.md || true)"</automated>
    <fails_when>non-zero exit, which happens when a deferred number marker is still in README.md, meaning a number was never filled</fails_when>
    <automated>grep -q '44' README.md &amp;&amp; grep -q '52' README.md &amp;&amp; grep -q '686' README.md</automated>
    <fails_when>non-zero exit, which happens when the corpus program count, the recovered form count or the control count is absent from README.md</fails_when>
    <automated>test 0 -eq "$(LC_ALL=C tr -d '\11\12\40-\176' < README.md | wc -c | tr -d ' ')"</automated>
    <fails_when>non-zero exit, which happens when filling a number introduced a byte outside tab, newline and printable ASCII</fails_when>
  </verify>
  <acceptance_criteria>
    - All five acceptance commands pass, and the count of tests each reported is
      recorded in the plan summary.
    - `README.md` holds zero occurrences of the deferred number marker string.
    - Every number in `README.md` is written as a pair of counts, recovered against
      declared. No single derived figure stands in place of a pair.
    - `README.md` names `tests/ratios.toml` as the source of the pinned numbers.
    - `README.md` explains the one refusing form in one sentence, so the reader
      reads a designed refusal and not a failure.
    - `README.md` still holds no byte outside tab, newline and printable ASCII.
  </acceptance_criteria>
  <done>
    All 44 corpus programs are measured, and every number in the README is a number
    a test reported on this run.
  </done>
</task>

<task type="auto">
  <name>Task 2: The honesty audit</name>
  <files>README.md</files>
  <read_first>
    - `README.md` as it now stands, in full
    - `CHANGELOG.md` and `LICENSES.md` (written by plan 06-05)
    - `.planning/research/STRUCTURES.md` section 11 and
      `.planning/research/FILE-FORMATS.md` section 9, both in full. These are the
      two registers the README's limit tables were built from.
    - `.planning/phases/06-version-1-0/06-06-SUMMARY.md`, which records every
      sentence on the claim surface check's audited allow list and the reason for
      each. Review the list, not the regular expressions.
    - `06-VALIDATION.md`, the "Manual-Only Verifications" table
  </read_first>
  <action>
    Run the whole measurement, in this order, and record each result:

    1. `cargo fmt --all --check`, `cargo clippy --all-targets -- -D warnings` and
       `cargo test --workspace`, the three gate commands.
    2. `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace`, which must
       exit zero.
    3. `sh scripts/check-claim-surface.sh`, which must exit zero and print its
       count of planted violations that fired.
    4. `cargo run -q -p xtask -- licences` followed by
       `git diff --exit-code -- LICENSES.md`, which must be clean.
    5. The hostile input re-verification, which is what SAF-01, SAF-04 and SAF-05
       rest on: `cargo test -p deform6 --test fuzz_smoke`,
       `cargo test -p deform6 --test no_panic_proof`,
       `cargo test -p deform6 --test regressions`,
       `cargo test -p deform6 --test salvage`,
       `cargo test -p deform6 --test refusal` and
       `cargo test -p deform6 --test severity_census`.
    6. The three named edge families this plan's truths rest on, run by name so the
       summary can cite them:
       `cargo test -p deform6 --lib two_names_that_clamp_to_the_same_forty_bytes_are_made_distinct_by_the_issuer`,
       `cargo test -p deform6 --lib a_thousand_controls_with_the_same_raw_name_each_get_a_distinct_name`,
       `cargo test -p deform6 --lib two_objects_whose_raw_names_collide_name_two_different_files`,
       `cargo test -p deform6 --lib gui_table_refuses_an_implausible_form_count`,
       `cargo test -p deform6 --test salvage -- two_salvage_runs_over_the_same_bytes_give_byte_identical_json`
       and
       `cargo test -p deform6 --test ratios -- editing_only_the_ratio_fails_because_it_disagrees_with_its_own_counts`.

    Then walk every claim in `README.md`, sentence by sentence, and record in the
    plan summary which measured result or cited fact each capability sentence rests
    on. A sentence with no anchor in a `tests/ratios.toml` total, a `Confidence`
    variant name, or a `limits` string the report emits verbatim does not belong in
    the file. Remove it or repair it, and say in the summary which you did.

    Review the claim surface check's audited allow list. Every entry must be a
    negation or a definition. An entry that is a real capability claim with the
    forbidden shape inside it is the failure mode this list can hide, so name each
    entry and its reason in the summary.
  </action>
  <verify>
    <automated>cargo fmt --all --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --workspace</automated>
    <fails_when>non-zero exit from any of the three, or clippy printing a line beginning "error:"</fails_when>
    <automated>RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace</automated>
    <fails_when>non-zero exit; measured at plan time this command exits 101 on the tree before plan 06-03 ran, so a zero exit is the evidence the repair held</fails_when>
    <automated>sh scripts/check-claim-surface.sh</automated>
    <fails_when>non-zero exit, or the closing output does not name the count of planted violations that fired</fails_when>
    <automated>cargo run -q -p xtask -- licences &amp;&amp; git diff --exit-code -- LICENSES.md</automated>
    <fails_when>non-zero exit from either half; a non-empty diff means the committed licence file is not what a fresh re-derivation produces</fails_when>
    <automated>cargo test -p deform6 --test fuzz_smoke &amp;&amp; cargo test -p deform6 --test no_panic_proof &amp;&amp; cargo test -p deform6 --test regressions &amp;&amp; cargo test -p deform6 --test salvage &amp;&amp; cargo test -p deform6 --test refusal &amp;&amp; cargo test -p deform6 --test severity_census</automated>
    <fails_when>non-zero exit from any of the six, or any summary line reading "0 passed"</fails_when>
    <automated>cargo test -p deform6 --lib two_objects_whose_raw_names_collide_name_two_different_files</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed" because the filter matched no test</fails_when>
    <automated>cargo test -p deform6 --test ratios -- editing_only_the_ratio_fails_because_it_disagrees_with_its_own_counts</automated>
    <fails_when>non-zero exit, or the summary line reads "0 passed" because the filter matched no test</fails_when>
    <human-check>
      Success criterion 4 is a prose cross-reference, and string matching against
      prose passes for the wrong reason. Do this by reading.

      Open `.planning/research/STRUCTURES.md` section 11 and
      `.planning/research/FILE-FORMATS.md` section 9 beside `README.md`. For each
      row that is still open, find its entry in the README's limit table and
      confirm two things: the entry names the same gap the register names, and the
      stated default matches what the code really does.

      The counts already match, because plan 06-02 made that an automated check.
      What this read adds is whether each row says the right thing, which no count
      can tell you.

      Answer: approved, or name each row whose default is wrong or missing.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The three gate commands pass.
    - `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace` exits zero.
    - `sh scripts/check-claim-surface.sh` exits zero and prints its planted
      violation count.
    - Re-deriving `LICENSES.md` changes nothing.
    - All six hostile input test files pass.
    - The six named edge tests pass, each matched by name, and each reports at
      least one test rather than zero.
    - The plan summary records, per README capability sentence, the measured result
      or cited fact it rests on.
    - The plan summary names every entry on the claim surface check's audited allow
      list and the reason it is there.
    - A human has read the two registers against the README limit tables and
      recorded an approval or a list of wrong rows.
  </acceptance_criteria>
  <done>
    Every released claim is traced to a measured result or a cited fact, and the
    whole gate is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: The v1.0.0 tag, created locally and pushed nowhere</name>
  <files>(no file is modified; this task creates the annotated git tag v1.0.0)</files>
  <read_first>
    - `.planning/phases/06-version-1-0/06-04-SUMMARY.md`, which records the exact
      version string and tag string the decision checkpoint confirmed. Use those
      strings. Do not re-derive them.
    - `scripts/check-release-version.sh` (written by plan 06-04), including its
      no-argument path, which reads the tag from `git describe --tags --exact-match`
      and refuses when HEAD carries none.
    - `AGENTS.md` section "Who writes a commit" and section "Commits". Measured at
      plan time, this repository's git identity is already set correctly, so do not
      change it.
  </read_first>
  <action>
    Create the annotated tag `v1.0.0` on the commit that holds the green
    measurement from task 2. Use `git tag -a v1.0.0` with a message that states
    what the release is, in one or two sentences, in the present tense.

    Name no author in the tag message. Add no co-author line, no session link and
    no footer saying a tool produced the text. `AGENTS.md` states the reason.

    Push nothing. Do not push the tag, do not push the branch, and do not open a
    pull request. A tag that is pushed can be deleted but cannot be recalled from
    anyone who already fetched it, and the human has not asked for a push. Say in
    the plan summary that the tag exists locally and name the one command the human
    runs to publish it, so publishing stays a decision and not a side effect.

    Then run `sh scripts/check-release-version.sh` with no argument. It reads the
    tag from `git describe --tags --exact-match` and compares it against the version
    in `Cargo.toml`. This is success criterion 5's last clause, measured rather than
    asserted.
  </action>
  <verify>
    <automated>t=$(git tag -l v1.0.0) || exit 1; test "v1.0.0" = "$t"</automated>
    <fails_when>non-zero exit, which happens when the tag does not exist</fails_when>
    <automated>test "tag" = "$(git cat-file -t v1.0.0)"</automated>
    <fails_when>non-zero exit, which happens when `v1.0.0` is a lightweight tag pointing straight at a commit rather than an annotated tag object</fails_when>
    <automated>sh scripts/check-release-version.sh</automated>
    <fails_when>non-zero exit, which happens when the tag on HEAD and the version in Cargo.toml disagree, or when HEAD carries no tag at all</fails_when>
    <automated>m=$(git tag -l --format='%(contents)' v1.0.0) || exit 1; test 0 -eq "$(printf '%s\n' "$m" | grep -ciE 'co-authored-by|generated with|claude|assistant' || true)"</automated>
    <fails_when>non-zero exit, which happens when the tag message names a tool or an agent as an author</fails_when>
    <automated>r=$(git ls-remote --tags origin 2>/dev/null || true); test 0 -eq "$(printf '%s\n' "$r" | grep -c 'refs/tags/v1.0.0' || true)"</automated>
    <fails_when>non-zero exit, which happens when the tag reached a remote; this task pushes nothing, so a remote hit means something published it</fails_when>
  </verify>
  <acceptance_criteria>
    - `git tag -l v1.0.0` prints the tag, and `git cat-file -t v1.0.0` prints `tag`,
      so it is annotated rather than lightweight.
    - `sh scripts/check-release-version.sh` run with no argument exits zero.
    - The tag message names no author, no co-author, no tool and no session.
    - No remote holds `refs/tags/v1.0.0`.
    - The plan summary names the exact command the human runs to publish the tag,
      and states that this plan did not run it.
  </acceptance_criteria>
  <done>
    The tag exists locally, it matches the version in `Cargo.toml`, and nothing was
    pushed.
  </done>
</task>

</tasks>

## Flagged assumptions from the edge probe

Four `unclassified` probe rows belong to this plan's requirements. The probe could
not classify them, so they are not auto-resolved and they are not dismissed.

| Requirement | Probe row | Why it stays unresolved |
|---|---|---|
| DET-02 | `unclassified` — review manually | The probe raised no shaped edge for "the tool follows the entry point stub to the VB header and confirms the `VB5!` signature". The register row S-18 covers the entry point stub variants `0x5A` and `0x11`, which the README lists as an open limit: the tool refuses an exotic stub rather than guessing it. No corpus program carries one, so the branch has no measured edge. |
| SAF-01 | `unclassified` — review manually | The probe raised no shaped edge for "the tool does not panic on any input". This is a whole program property, not a shaped edge, and it is proved by the fuzzer, by `no_panic_proof.rs` and by the clippy deny wall rather than by one acceptance criterion. Task 2 re-runs all three. The residual risk is the input nobody has generated yet, which is what the fuzz campaign exists for. |
| VER-04 | `unclassified` — review manually | The probe raised no shaped edge for "what the compiler does not keep is excluded by a written rule". The exclusion rules are prose in `crates/deform6/tests/support/`, and whether the rule set is complete cannot be measured against a corpus that never held the excluded thing. The README states the rule set exists; it does not claim the set is exhaustive. |
| WRT-07 | `unclassified` — review manually | The probe raised no shaped edge for "a procedure whose body cannot be recovered is written as a stub". `extract_structural.rs` proves the stub is written and is shaped correctly. What a reader does with a project full of stubs is a product question the README answers in prose, not a measurable edge. |

## Edge-probe ledger

The deterministic edge probe raised 107 applicable rows across the 42 requirement
identifiers, all `unresolved` with `verification: null`. Every one is accounted
for below. No row was dismissed and no row was dropped.

**Rows authored as explicit truths: 97.** They collapse into seven truths, one per
probe category, because Phase 6 re-verifies behaviour that Phases 1 to 5 already
decided and already tested. Each truth names every requirement whose row it
covers.

| Category | Rows | Truth authored in | Requirements covered |
|---|---|---|---|
| `empty` | 26 | `06-07` `must_haves.truths` | DET-03, DET-04, DET-06, FRM-01, FRM-02, FRM-03, FRM-04, FRM-06, OBJ-01, OBJ-03, OBJ-04, OBJ-06, RPT-02, RPT-03, RPT-04, RPT-05, SAF-02, SAF-04, SAF-05, VER-01, VER-02, VER-03, VER-05, VER-06, WRT-02, WRT-03 |
| `encoding` | 19 | `06-02` `must_haves.truths` | DET-03, DET-04, DET-06, FRM-01, FRM-02, FRM-03, FRM-04, FRM-06, OBJ-01, OBJ-03, OBJ-04, OBJ-06, RPT-04, SAF-02, SAF-04, VER-03, VER-05, VER-06, WRT-03 |
| `adjacency` | 16 | `06-07` `must_haves.truths` | DET-04, FRM-01, FRM-02, FRM-03, FRM-06, OBJ-01, OBJ-03, OBJ-04, RPT-02, RPT-03, RPT-04, RPT-05, SAF-05, VER-01, VER-02, WRT-02 |
| `ordering` | 16 | `06-07` `must_haves.truths` | DET-04, FRM-01, FRM-02, FRM-03, FRM-06, OBJ-01, OBJ-03, OBJ-04, RPT-02, RPT-03, RPT-04, RPT-05, SAF-05, VER-01, VER-02, WRT-02 |
| `concurrency` | 12 | `06-02` `must_haves.truths` | DET-01, DET-04, DET-05, FRM-06, OBJ-05, SAF-02, SAF-03, SAF-04, VER-02, VER-03, WRT-04, WRT-05 |
| `boundary` | 4 | `06-07` `must_haves.truths` | FRM-02, FRM-03, FRM-06, RPT-03 |
| `precision` | 4 | `06-07` `must_haves.truths` | FRM-02, FRM-03, FRM-06, RPT-03 |

**Rows authored as backstop truths: 0.** Every one of the seven families has a
named, shipped test or a checkable README sentence behind it, so an explicit
acceptance criterion was defensible in every case. No family needed the backstop
marker.

**Rows surfaced as flagged assumptions: 10.** All ten are the `unclassified`
category, which is never auto-resolved.

| Requirement | Plan that owns it |
|---|---|
| RPT-01 | 06-01 |
| RPT-06 | 06-01 |
| OBJ-02 | 06-03 |
| FRM-05 | 06-04 |
| WRT-01 | 06-04 |
| WRT-06 | 06-04 |
| DET-02 | 06-07 |
| SAF-01 | 06-07 |
| VER-04 | 06-07 |
| WRT-07 | 06-07 |

**The arithmetic.** 97 explicit plus 0 backstop plus 10 flagged assumptions equals
107, which is the probe's own `coverage.applicable` count.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a published tag -> everyone who fetches it | A tag cannot be recalled from a fetcher. This plan creates one locally and pushes nothing. |
| the acceptance run -> the local file system | 44 project directories are written during the run |
| an untrusted `.exe` -> the library reader | All 44 corpus programs and every fuzz regression input run through both modes |
| the released README -> the reader | The final claim surface, with the numbers filled |

## STRIDE Threat Register

ASVS level 1. Blocking severity: high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07-01 | Repudiation | publishing the tag | high | mitigate | Nothing is pushed. A counted check confirms no remote holds `refs/tags/v1.0.0`, and the plan summary names the one command the human runs to publish it, so publishing stays a decision. |
| T-06-07-02 | Tampering | the 44 program acceptance run's writes | high | mitigate | The shipped `extract` containment check refuses the whole run when a planned path is not a direct child of the resolved output directory, and a second check refuses when a symbolic link already sits at a planned path. `extract_structural.rs` writes into a fresh temporary directory per program. This plan adds no new write path. |
| T-06-07-03 | Denial of Service | hostile input re-verification | high | mitigate | `fuzz_smoke`, `no_panic_proof`, `regressions`, `salvage`, `refusal` and `severity_census` all run in task 2. The quadratic time path issuer fault Phase 5 found and fixed is covered by the regression inputs, which replay in both modes. |
| T-06-07-04 | Repudiation | a number in the README | high | mitigate | Every number is the pair of counts the pinned ratio gate asserts, taken from the test run and not from a planning document. A counted check holds the README to zero remaining deferred markers. |
| T-06-07-05 | Spoofing | the tag and the manifest version | high | mitigate | `scripts/check-release-version.sh` runs with no argument and compares `git describe --tags --exact-match` against `Cargo.toml`. It refuses when HEAD carries no tag rather than passing silently. |
| T-06-07-06 | Tampering | the annotated tag message | medium | mitigate | A counted check rejects a co-author line, a tool name or a session link in the tag message, and `git cat-file -t` confirms the tag is annotated rather than lightweight. |
</threat_model>

<verification>
- All five success criterion 1 checks pass over all 44 corpus programs.
- The three gate commands pass, `cargo doc` produces no warning, and the claim
  surface check passes with its planted violations firing.
- `README.md` holds zero deferred number markers and every number is a pair of
  counts.
- `v1.0.0` exists as an annotated local tag, matches `Cargo.toml`, and reached no
  remote.
- A human has read the two gap registers against the README limit tables.
</verification>

<success_criteria>
- Success criterion 1 is met and measured: 44 of 44 extract, 44 of 44 structural,
  44 of 44 schema, and every pinned ratio unchanged.
- Success criterion 5 is met in full: the three gate commands pass, `cargo doc
  --no-deps` produces no warning, and the tag matches the version in `Cargo.toml`.
- Success criterion 2 is re-confirmed on the finished README, not only on the
  draft.
- The phase ships a release that states what was run, and nothing more.
</success_criteria>

<output>
Create `.planning/phases/06-version-1-0/06-07-SUMMARY.md` when done. Record the
five filled numbers, the result of every command in task 2, the audited allow list
review, and the exact command the human runs to publish the tag.
</output>

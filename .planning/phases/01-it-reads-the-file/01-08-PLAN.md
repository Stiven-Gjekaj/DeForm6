---
phase: 01-it-reads-the-file
plan: 08
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - crates/deform6-cli/src/main.rs
  - crates/deform6-cli/tests/cli.rs
  - crates/deform6/tests/refusal.rs
  - crates/deform6/tests/corpus_sweep.rs
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-04, DET-05, DET-06]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`deform6 inspect` on a corpus program prints the locked eight-line shape, exits 0, and changes no file on disk."
    - "Argument parsing never lets clap choose the exit code. A usage error exits 5, not 2."
    - "`--help` and `--version` exit 0."
    - "A VB5 file, a .NET file and an empty file each give a distinct refusal variant and a distinct exit code."
    - "All 44 corpus executables resolve the entry point to a file offset, reach a `VB5!` signature, name `MSVBVM60.DLL`, and report native."
    - "The sweep asserts the file count before it loops, so an empty list cannot pass."
    - "No test asserts on the wording of a refusal sentence. Every refusal test asserts on the variant or on the exit code."
  artifacts:
    - crates/deform6-cli/src/main.rs
    - crates/deform6-cli/tests/cli.rs
    - crates/deform6/tests/refusal.rs
    - crates/deform6/tests/corpus_sweep.rs
  key_links:
    - "`CARGO_BIN_EXE_deform6` comes from the `[[bin]] name = \"deform6\"` line plan 01-01 wrote. It is the bin target name, not the package name."
    - "`PeImage::dll_name_sites` from plan 01-04 is what makes the refusal fixtures possible without any binary entering the repository."
    - "`Report.native` and `Report.project_name` from plan 01-07 are the two fields the sweep reads."
---

<objective>
Build the `deform6-cli` shell: the `inspect` subcommand, the six exit codes, the
locked eight-line output shape, and the three test files that turn this phase's
success criteria into commands.

Purpose: DET-06 and the proof of DET-01 through DET-05. Everything before this
plan is a library that returns values. This plan is the shell that turns a value
into a number and a sentence, plus the gate that measures the whole phase.

Output: the command line binary and three test files, none of which puts a
third-party binary into the repository.
</objective>

<user_decisions>
| ID | Decision | Source |
|----|----------|--------|
| D-01 | `inspect` prints human-readable text only in this phase. There is no `--json` yet. Phase 4 introduces the machine-readable surface. One schema, introduced once. | CONTEXT.md, "`inspect` prints text" |
| D-02 | The exit code names the reason: 0 read, 1 not a PE file, 2 a PE file with no Visual Basic runtime, 3 a Visual Basic runtime that is not version 6, 4 Visual Basic 6 but damaged, 5 an internal error. It makes the refusal tests assert on a number rather than on the wording of a message. | CONTEXT.md, "The exit code names the reason" |
| D-03 | `Cli::try_parse` with a hand-written mapping is required, not a preference. clap exits 2 on a usage error and exit code 2 is locked as "a PE file with no Visual Basic runtime". A usage error maps to code 5. | CONTEXT.md, "clap already uses exit code 2, so the CLI must not let it exit" |
| D-05 | `inspect` prints the object count in this phase, from `wCompiledObjects`. | CONTEXT.md, "`inspect` prints the object count in this phase" |
| D-07 | Do not claim the P-code branch is tested. Say so in the test, do not paper over it. | CONTEXT.md, "What this phase must not do" |
</user_decisions>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-it-reads-the-file/CONTEXT.md
@.planning/phases/01-it-reads-the-file/RESEARCH.md
@.planning/phases/01-it-reads-the-file/01-03-SUMMARY.md
@.planning/phases/01-it-reads-the-file/01-06-SUMMARY.md
@.planning/phases/01-it-reads-the-file/01-07-SUMMARY.md
</context>

<precondition>
Plan 01-01 wrote `[[bin]] name = "deform6"` into `crates/deform6-cli/Cargo.toml`,
so cargo sets `CARGO_BIN_EXE_deform6` for an integration test in that package.
The variable is named for the bin target, not for the package, so it is not
`CARGO_BIN_EXE_deform6_cli`.
</precondition>

<execution_note>
This is the largest plan in the phase: three tasks, four new files, and the only
plan that writes both a binary and its integration tests. Its estimate is the
highest of the eight. It is deliberately not split, because the ROADMAP fixes
this phase at eight plans and the three tasks are one coherent unit: the CLI and
`cli.rs` must share a commit under AGENTS.md, and the other two files are the
phase gate.

Watch for the split during execution. If context runs short, stop after task 1,
write the SUMMARY covering what is done, and hand `refusal.rs` and
`corpus_sweep.rs` to a continuation. Those two files depend only on the library,
not on task 1, so they can be finished separately without rework. Do not drop a
behaviour to fit; drop a task boundary instead.
</execution_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The inspect subcommand, the six exit codes, and the locked output shape</name>
  <files>crates/deform6-cli/src/main.rs, crates/deform6-cli/tests/cli.rs</files>
  <read_first>.planning/phases/01-it-reads-the-file/CONTEXT.md, "`inspect` prints text" and "The exit code names the reason" and "clap already uses exit code 2"; .planning/phases/01-it-reads-the-file/RESEARCH.md sections 7.6, 7.7, 7.8; .planning/phases/01-it-reads-the-file/01-03-SUMMARY.md for the refusal-to-code table</read_first>
  <behavior>
    - `deform6 inspect corpus/vb6-code/Mandelbrot/Mandelbrot.exe` exits 0.
    - Its stdout holds eight lines whose labels are, in order, File, Format, Runtime, Header, Project, Title, Mode, Objects.
    - The Project line names the project the `.vbp` beside the executable declares, and the Mode line reads native.
    - A directory listing of the executable's directory, taken as a set of path, length and modification time before and after the run, is identical afterwards.
    - `deform6 inspect` on a short text file the test writes into the temporary directory exits 1, writes nothing to stdout, and writes exactly one non-empty line to stderr.
    - Every test in this file that expects a non-zero exit code also asserts that stderr holds exactly one non-empty line. A refusal with an empty stderr is a tool that told the user nothing, and DET-04 asks for one clear sentence.
    - `deform6 inspect` on a path that does not exist exits 5.
    - `deform6 frobnicate` exits 5 and not 2.
    - `deform6` with no arguments exits 5 and not 2.
    - `deform6 --help` exits 0 and `deform6 --version` exits 0.
  </behavior>
  <action>
Write `crates/deform6-cli/src/main.rs` and `crates/deform6-cli/tests/cli.rs` in one task and one commit, because AGENTS.md puts the code and its tests in the same commit.

Define the `Exit` enum exactly as RESEARCH.md section 7.8 gives it: `#[repr(u8)]`, deriving `Clone, Copy, Debug, PartialEq, Eq`, with `Ok = 0`, `NotPe = 1`, `NoVbRuntime = 2`, `NotVb6 = 3`, `Damaged = 4`, `Internal = 5`. Per D-02, define all six now even though `Damaged` has no `--salvage` route until Phase 5, so the numbering never moves. `Damaged` is in fact already reachable, because a truncated file produces the damaged refusal.

Write `impl From<Exit> for ExitCode` using `e as u8`. That cast passes the lint wall: `clippy::cast_possible_truncation` does not fire on an enum-to-integer cast on a `#[repr(u8)]` fieldless enum, which RESEARCH.md section 7.8 measured.

Return `ExitCode` from `main`. Do not call the standard library's process-exit function anywhere. It does not run destructors, and it would make a future buffered-writer flush silently disappear.

Per D-03, parse with `Cli::try_parse` and map the error yourself. On an error, print it with the error's own `print` method, which sends help and version to stdout and a usage error to stderr exactly as clap would have, then return `Exit::Ok` for the `DisplayHelp` and `DisplayVersion` error kinds and `Exit::Internal` for everything else. The error-kind enum is available with the `error-context` feature plan 01-01 already selected.

<!-- planner-discipline-allow: Cli::parse, process::exit -->

Never call the non-fallible clap parse entry point. It calls the process-exit function itself, so no mapping of ours can run after it, and it uses status 2 for a usage error, which collides with the locked meaning of code 2. A user who mistypes a flag would get the code that means "this file is not Visual Basic", which is worse than no code at all. The acceptance command greps `crates/deform6-cli/` for a call to that entry point and for a call to the process-exit function, so do not write either name followed by an open parenthesis anywhere in that crate, including in a comment. If you want to warn a reader in a comment, name the functions without their parentheses.

The `Cli` type is a clap derive struct with one subcommand, `Inspect`, taking one positional argument. Take the path as a `PathBuf` from an `OsString`, never as a `String`: a path is not required to be UTF-8. Per D-01 add no `--json` flag, no `--salvage` flag and no output-format option. Phase 4 and Phase 5 add those.

`run_inspect` reads the file with the standard library's read-to-vector call. An input-output error maps to `Exit::Internal`, code 5, with one sentence to stderr naming the path and the reason. That is what makes the missing-file behaviour code 5 rather than code 1: the file was never read, so nothing about its format is known.

Then call `deform6::inspect` on the bytes. On the error side, print one sentence to stderr, which is the message plan 01-03 attached to the refusal variant, and map the variant to a code using the table in the 01-03 SUMMARY: the three not-a-PE variants map to 1, the no-runtime variant maps to 2, the two wrong-version variants map to 3, and the damaged variant maps to 4. Write the mapping as an exhaustive match with no wildcard arm, so a variant added later is a compile error until its code is decided.

When the no-runtime variant carries a true .NET flag, the sentence says the file is a .NET assembly. The exit code is still 2. RESEARCH.md section 7.4 recommends exactly this: use the CLR header check in the message text under exit code 2, and add neither a variant nor a sixth code, because the exit-code table is a locked decision.

On the success side, print the locked eight-line shape from CONTEXT.md to stdout, in this order and with these labels: File, Format, Runtime, Header, Project, Title, Mode, Objects. The File line gives the file name and the byte length in parentheses. The Format line gives PE32 and the section count. The Runtime line gives the imported runtime DLL name followed by the version in parentheses. The Header line gives the four signature characters, the file offset of the header in eight hexadecimal digits, and the runtime build number in hexadecimal. The Project line gives the project name. The Title line gives the project title. The Mode line gives native or p-code. The Objects line gives the compiled object count, per D-05.

<!-- planner-discipline-allow: MSVBVM60, VB5! -->

Take the runtime DLL name and the four signature characters from the report's `runtime_dll` and `signature` fields, which plan 01-07 filled from the file. Do not write either of those strings into this crate as a literal, in code or in a comment. The acceptance command greps `crates/deform6-cli/src/` for the bare text of both. This is not style. The report is the only thing that knows what the file actually held, and a literal here would print the same two values for a file that held something else, which is a wrong answer stated confidently.

Pin the shape in the test, never the values. RESEARCH.md's Pitfall 6 records that the example output in CONTEXT.md is illustrative and that its byte count, section count and build number do not match the real file. Assert that eight lines appear with those eight labels in that order, and assert the project name and the word native, which the `.vbp` beside the executable proves. Do not assert a size, a section count, a header offset or a build number anywhere.

Write the nine behaviours above into `crates/deform6-cli/tests/cli.rs`. Open the file with the inner attribute block from RESEARCH.md section 2.5 verbatim, using `#[allow]` and never `#[expect]`: an unfulfilled expectation is itself a build error under the deny wall, so a test that stops using one of the relaxed lints would break the build for the wrong reason.

Assert the sentence, not its wording. On every refusal path, check that stderr trimmed of its trailing newline is non-empty and holds no interior newline, and that stdout is empty. That is DET-04's "one clear sentence" made mechanical: plan 01-03 proves each variant renders one line, and this proves the shell actually writes it. Do not compare stderr against an expected string; CONTEXT.md gives the exit-code table precisely so a reworded sentence does not fail the suite and a changed meaning does.

Spawn the binary through `CARGO_BIN_EXE_deform6`. Read the exit code with the status's code accessor, which returns an optional value and is empty when the process was killed by a signal; use `expect` with a message saying the process was not signalled, so a crash reports as that rather than as a wrong number. Pass the captured stderr into every assertion message, because when code 1 arrives instead of 0 the sentence the tool printed is the whole diagnosis.

The fourth behaviour is ROADMAP success criterion 1's "changes no file on disk" half. Snapshot every entry under the executable's directory as a set of path, length and modification time, run the binary, and compare the two sets. Do not shell out to a directory-listing program: AGENTS.md says to measure the thing you tell the human, and a size is not a state.

Then break the covered behaviour on purpose. Replace the fallible parse with the non-fallible one, run `cargo test -p deform6-cli`, and confirm both the bad-subcommand test and the no-arguments test fail with code 2. Restore it. Then map the damaged refusal to code 1, and confirm nothing fails, because no test in this file feeds a truncated file to the binary; add one that does, feeding a half-length copy of the corpus file written into the temporary directory, and confirm it then fails. Restore the mapping, re-run, and record all three outcomes in the SUMMARY, including the one that produced no failure, because a breakage that produces no failure is the finding.
  </action>
  <verify>
    <automated>cargo test -p deform6-cli && cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && ! grep -rnE '(Cli::parse|process::exit)[[:space:]]*\(' crates/deform6-cli/ && ! grep -rn 'json' crates/deform6-cli/src/main.rs && ! grep -rnE 'MSVBVM60|VB5!' crates/deform6-cli/src/</automated>
  </verify>
  <done>
All ten behaviours pass, including the added truncated-file behaviour. A usage error and a bad subcommand each exit 5. `--help` and `--version` each exit 0. The directory listing is identical before and after the run. `crates/deform6-cli/` holds no call to the non-fallible clap parse entry point and no call to the process-exit function. The three deliberate breakages were run and all three outcomes are recorded in the SUMMARY. The gate is green.

Commit subject: `Add the inspect subcommand and the exit code mapping`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: refusal.rs - every refusal, from fixtures built inside the test</name>
  <files>crates/deform6/tests/refusal.rs</files>
  <read_first>.planning/phases/01-it-reads-the-file/RESEARCH.md sections 7.1, 7.2, 7.3, 7.4; AGENTS.md sections "What may enter this repository" and "What a test may hold on to"</read_first>
  <behavior>
    - An empty file gives `Refusal::NotPe`.
    - A short text file gives `Refusal::NotPe`.
    - A corpus file with its imported runtime name patched to `KERNEL32.DLL` gives `Refusal::NoVbRuntime`.
    - A corpus file with its imported runtime name patched to `mscoree.dll` followed by a NUL gives `Refusal::NoVbRuntime` with `dot_net` false. Naming an assembly's loader shim in the import table does not make a file a .NET assembly, and this fixture proves only that a second unrecognised name is still refused.
    - A corpus file with its imported runtime name patched to `mscoree.dll` **and** its data directory 14 set to a non-zero address and size gives `Refusal::NoVbRuntime` with `dot_net` true. This is the only place in the phase where the .NET flag is observed end to end.
    - A corpus file with its imported runtime name patched to `MSVBVM50.DLL` gives `Refusal::IsVb5`.
    - A corpus file with its imported runtime name patched to `VB40032.DLL` followed by a NUL gives `Refusal::IsVb4`.
    - A corpus file truncated to half its length gives `Refusal::Damaged`, which is a different variant from `Refusal::NotPe`.
    - The unmodified corpus file is accepted. This is the control.
  </behavior>
  <action>
Write `crates/deform6/tests/refusal.rs` with the eight tests RESEARCH.md section 7.2 names, one function per refusal, named for the behaviour rather than for the variant, plus two helpers at the top. No module nesting: the file is small and the file-level relaxation covers it.

Open the file with the inner attribute block from RESEARCH.md section 2.5 verbatim. It is an inner attribute, because an integration test file is its own crate root. Use `#[allow]`, never `#[expect]`.

The first helper is a four-line `corpus_root` that joins the manifest directory with the relative path to the repository's `corpus/`. Duplicate it rather than sharing one across the three test files: RESEARCH.md's Wave 0 gaps note says a shared support module would be a dependency between three tests that must be able to fail independently.

The second helper patches an imported DLL name in memory, at the offset the PE itself names. Take the sites from `PeImage::dll_name_sites`, which reads the import descriptor and maps its name RVA through the section table. It is not a byte search. A byte search for the runtime name could hit the same bytes in the resource section or in a string table, and AGENTS.md names that class of mistake directly. Assert inside the helper that the replacement is exactly as long as the name it replaces, so no offset moves and a wrong-length replacement is a loud failure rather than a corrupted image. The Visual Basic 6 runtime name is 12 bytes; two of the replacements are 11, so each carries its own trailing NUL and the original NUL becomes a second, harmless one.

The third helper sets data directory 14. It computes the entry's file offset from the headers exactly as plan 01-04's positive `has_clr_header` test does: read `e_lfanew` as a `u32` at file offset 0x3C, add 24 to reach the optional header, then add 96 plus 14 times 8 to reach the fifteenth data directory, which is an address and a size. Write the first section's virtual address and the size 0x48. Assert both values were zero before the write.

This helper is why `Refusal::NoVbRuntime` carries a `dot_net` flag at all. Without it, no test anywhere in the phase ever calls `PeImage::has_clr_header` on an input that has a CLR header, the flag is always false, and the fifth and sixth behaviours above would be byte-identical results with different names. ROADMAP success criterion 3 asks that a .NET executable produce a distinct result, and this is the fixture that makes that observable rather than asserted.

Name the two fixtures for what they are. The name-only one is a second no-runtime case and its test name must say so, for example `an_unrecognised_import_name_is_refused_as_holding_no_visual_basic_runtime`. Only the one that sets directory 14 may be named for .NET.

Nothing is written to disk. The bytes exist for the duration of one test function, in a vector. This is what AGENTS.md permits: it forbids a binary, a source file or a derived fixture from a third-party system entering the repository, including a fixture calculated from such a file, and nothing enters. The source is a `corpus/` program already committed under a redistributable licence with its origin recorded in `corpus/NOTICES`.

Assert on the variant, never on the sentence. CONTEXT.md gives that as a reason for the exit-code table: a reworded sentence must not fail the suite, and a changed meaning must.

The eighth test is the control and it is not optional. Without it, a bug that made the library refuse everything would leave all seven refusal tests green. AGENTS.md warns about exactly that failure shape under "a test can pass for the wrong reason".

Do not add a `Refusal::IsDotNet` variant. RESEARCH.md section 7.4 recommends the CLR header check as a message enrichment under exit code 2 and against a new variant, because a new variant would need a sixth exit code or an overload of code 2, and the exit-code table is locked. ROADMAP success criterion 3 asks for a distinct result from a VB5 file, a .NET file and an empty file, and the phase gives four distinct results across those inputs: the VB5 variant, the not-a-PE variant, and the no-runtime variant in its two forms, which render two different sentences. Assert on all four.

Write a comment at the top of the file saying plainly what these fixtures prove and what they do not. A patched import name proves the discrimination logic. It does not prove that DeForm6 handles a genuine VB5 binary, whose header layout differs after 0x30, nor a genuine .NET assembly, which carries a CLR header the patched file does not. DeForm6 refuses both before it reads any of that, so the tests cover the behaviour this phase promises. When the fetched manifest gains a real VB5 sample in Phase 5, add a second test beside this one and do not delete this one: it runs offline and it runs on a clean clone.

Then break the covered behaviour on purpose, three times. Change the Visual Basic 5 name constant to the Visual Basic 6 one, run `cargo test -p deform6 --test refusal`, and confirm the VB5 test fails. Then delete the control test and confirm the suite still passes, which shows why the control has to be there; restore it. Then make `PeImage::has_clr_header` return a constant `false` and confirm the .NET fixture test fails while the name-only fixture test still passes, which is the evidence that the two are genuinely different inputs and not two names for one case. Restore it. Record all three outcomes in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p deform6 --test refusal && cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && ! grep -qE 'windows\(|\.position\(' crates/deform6/tests/refusal.rs && test -z "$(git status --porcelain corpus/)"</automated>
  </verify>
  <done>
All nine tests pass, including the control and the .NET fixture that sets data directory 14. Every fixture is built in memory from a committed corpus program and nothing is written to disk. The patch offsets come from the PE headers and from the import descriptor, never from a byte search. `git status --porcelain corpus/` is empty after the run. The three deliberate breakages were run and all three outcomes are recorded in the SUMMARY. The gate is green.

Commit subject: `Add the refusal tests built from patched fixtures`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: corpus_sweep.rs - all 44 executables, with the count asserted first</name>
  <files>crates/deform6/tests/corpus_sweep.rs</files>
  <read_first>.planning/phases/01-it-reads-the-file/RESEARCH.md sections 7.5, 8.5, and "Common Pitfalls" pitfall 6; .planning/research/STRUCTURES.md section 12</read_first>
  <behavior>
    - The walk finds exactly 44 executables under `corpus/`, and the count is asserted before the loop runs.
    - Every one of the 44 resolves its entry point to a file offset, and the report's `signature` field holds the four bytes of the Visual Basic magic, compared against a byte literal in the test.
    - Every one of the 44 carries `MSVBVM60.DLL` in the report's `runtime_dll` field, compared against the literal string. Do not assert on the `Runtime` enum: it has one variant, so comparing it against that variant is a tautology given `Ok`, and the sweep would then be asserting only that `inspect` did not fail.
    - Every one of the 44 reports native, and the assertion message says that this makes the P-code branch untested by construction.
    - Every one of the 44 returns a non-empty project name.
    - Every one of the 44 resolves all four header-relative strings, and the four offsets satisfy the ordering exe name, then title, then help file, then project name, with the last two allowed to be equal.
    - A failure anywhere in the loop is collected and the test reports every file that broke, not the first.
  </behavior>
  <action>
Write `crates/deform6/tests/corpus_sweep.rs` from the sketch in RESEARCH.md section 7.5. It is ROADMAP success criterion 2, written as a test rather than as a script, so `cargo test --workspace` is the whole gate and nothing has to be remembered.

Open the file with the inner attribute block from RESEARCH.md section 2.5 and its own four-line `corpus_root` helper.

Four properties the sweep must keep, and each of them is there because of a specific failure shape.

Assert the count first, as a literal 44, before the loop runs. A sweep over an empty list passes and proves nothing. The literal is the number the corpus documents record; when somebody adds a corpus program this test fails and forces the pinned numbers elsewhere to be looked at, and that failure is the point.

Walk recursively and compare the extension case-insensitively, and state the reason accurately. All 44 executables currently carry a lower-case `.exe`, so a case-sensitive match would miss none of them today. The corpus does already hold one file with an upper-case extension, `corpus/public-domain/SK-MCI-Sample__VB6/MCI.VBP`, which is a project file and not an executable; it is the evidence that this corpus is not uniformly lower-case, not evidence that a file is currently being missed. The case-insensitive compare is therefore a guard against a future addition, and the count assertion is what would catch such an addition either way. Do not write into the test a comment claiming a file is currently missed, because that would be false.

Collect failures rather than returning early. A run that names all the files that broke is worth ten runs that each name one. Build the message from the collected list and assert the list is empty.

When a directory cannot be read, fail with a message that names the directory. The file-level relaxation permits a panic in a helper, and a panic that names the path is a better failure report than a swallowed error.

For each file, call `deform6::inspect` on the bytes and check the six properties above.

The runtime check compares the report's `runtime_dll` string against the literal runtime name, and the signature check compares the report's `signature` bytes against a byte literal. Both of those literals belong in this test file and nowhere in `crates/deform6-cli/src/`, which plan 01-08 task 1 grep-gates: a test is where an expected value is written down, and a printer is not.

Do not assert on the `Runtime` enum instead. It has a single variant, so `assert_eq!(report.runtime, Runtime::Vb6)` cannot fail once `inspect` has returned `Ok`, and ROADMAP criterion 2 asks the sweep to observe the imported runtime name on each of the 44 files rather than to observe that the parse did not error. The four-string check reads the report's exe name, title, help file and project name, and requires all four to have resolved; the help file is allowed to be empty, because one corpus project has an empty help file, and the project name is not.

The offset-ordering check is the committed guard that keeps the 0x58 and 0x5C gap closed. RESEARCH.md section 8.5 measured that the ordering holds in every corpus file and that the largest header-relative offset seen is 171. Assert the ordering, and do not assert the value 171 or the value 0x78: those are observations about this corpus, not rules of the format, and section 8.5 says plainly to follow the offset in the field rather than to assume where the pool starts. The ordering assertion is a cheap sanity check that a reader can see was capable of failing, and it fails immediately if two of the four fields are ever swapped.

Per D-07, the native assertion's message must say what the result does and does not mean. Write it so that when a future corpus addition makes it fail, the reader is told the whole story: every one of the vendored projects carries a native compilation type, so the P-code branch of this field is untested by construction, and a P-code binary is needed before that branch can be called tested. Name the test so the same thing is visible in the test list without running it.

Do not compare the runtime build number, the file size or the section count against a literal anywhere. RESEARCH.md's Pitfall 6 records that the build number takes two different values across the corpus and that neither equals the value in the published sample.

The sweep runs in hundredths of a second, so do not gate it behind a feature flag or an ignore attribute.

Then break the covered behaviour on purpose, three times. Change the asserted count from 44 to 0, run `cargo test -p deform6 --test corpus_sweep`, and confirm the test fails on the count rather than passing over an empty list. Restore it. Then swap the exe-name and title fields in the report construction in `vb/mod.rs`, and confirm the ordering assertion fails naming the files. Restore it. Then make `vb/mod.rs` fill `runtime_dll` from the runtime name constant instead of from the value `runtime_of` returned, and confirm the sweep still passes; that is expected, because the corpus name equals the constant, so record it as the known limit of this assertion and note that plan 01-08 task 1's grep on the command line crate is what stops the same substitution happening where it would mislead a user. Restore it, re-run, and record all three outcomes in the SUMMARY, including how many of the 44 the ordering assertion named.
  </action>
  <verify>
    <automated>cargo test -p deform6 --test corpus_sweep && cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && grep -qE 'assert[^\n]*\b44\b' crates/deform6/tests/corpus_sweep.rs && grep -q 'MSVBVM60.DLL' crates/deform6/tests/corpus_sweep.rs && ! grep -qE '0x2636|28672|0x231C' crates/deform6/tests/corpus_sweep.rs</automated>
  </verify>
  <done>
All seven properties hold across all 44 corpus executables. The count is asserted before the loop. The runtime name and the signature bytes are compared against literals in this file, so the sweep observes what the parse read rather than that the parse returned. The walk is case-insensitive on the extension and the stated reason for that is accurate. Failures are collected, not returned early. No build number, file size or section count is compared against a literal. The three deliberate breakages were run and all three are recorded in the SUMMARY. The gate is green.

Note for the executor: the `grep` on the count assertion in the acceptance command is weak, because a comment holding the number would satisfy it. It is there as a cheap tripwire only. The deliberate breakage below is what actually proves the count guard, so do not skip it.

Commit subject: `Add the corpus sweep over all 44 executables`
  </done>
</task>

</tasks>

<reversibility rating="costly">
The exit-code table is a published contract: CONTEXT.md's stated reason for it
is that a loop over a directory of unknown files can sort them by number without
reading English. Changing a number later breaks any such loop silently. All six
codes are defined now so the numbering never moves, which is what CONTEXT.md
asks for.
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the command line -> the process | Argument parsing decides an exit code that a script may act on. |
| the file path -> the process | The path is attacker-influenced in the sense that a user points the tool at a directory of unknown files. |
| the process -> the file system | `inspect` must be provably read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-29 | Spoofing | the clap exit-code collision | high | mitigate | The fallible parse entry point plus a hand-written mapping. A usage error exits 5, not 2, so a script sorting a directory by exit code cannot file a typo under "not Visual Basic". Two tests assert the code is not 2, and the acceptance command greps the crate for a call to the non-fallible entry point. |
| T-01-30 | Tampering | `inspect` writing to disk | high | mitigate | The library takes a byte slice and names no file-system type, which plan 01-07 proves by grep. The shell opens one file for reading. A test snapshots path, length and modification time for every entry under the input's directory and compares the set before and after the run. |
| T-01-31 | Information disclosure | the refusal sentences and the printed report | low | mitigate | One sentence to stderr naming the reason and, for an input-output error, the path the user supplied. No file bytes are dumped and no host path the user did not supply is printed. ASVS V7 is the applicable category. |
| T-01-32 | Tampering | the input path argument | medium | mitigate | The path is taken as an operating-system string and held as a path buffer, so a non-UTF-8 path is not mangled and not rejected. Nothing is written in this phase, so path traversal has no target; Phase 4's `extract` is where path handling starts to matter. |
| T-01-33 | Repudiation | a test that passes for the wrong reason | high | mitigate | Three structural guards: the refusal suite carries a control test that a refuse-everything bug would fail, the sweep asserts its file count before it loops, and every refusal test asserts on a variant or a number rather than on a sentence. Each of the three was verified by deliberately breaking it. |
| T-01-SC | Tampering | third-party binary fixtures | high | mitigate | No fixture enters the repository. Every refusal input is built in memory from a committed corpus program by patching an imported name at the offset the PE itself names. `git status --porcelain corpus/` is empty after the suite runs. |
</threat_model>

<verification>
The whole phase gate, on a clean clone:

`cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace`

Then the three phase-level commands, each of which decides a ROADMAP success
criterion:

`cargo test -p deform6 --test corpus_sweep` decides criterion 2.
`cargo test -p deform6 --test refusal` decides criterion 3.
`cargo test -p deform6-cli --test cli` decides criterion 1.

<human-check>
Run `deform6 inspect corpus/vb6-code/Mandelbrot/Mandelbrot.exe` and read the
eight lines. Confirm the Project line names a project you recognise from the
`.vbp` beside the executable, that the Mode line reads native, and that the
labels are the eight CONTEXT.md locks. Then run the same command against a file
on your machine that is not a program at all, and confirm you get one sentence
that tells you why and an exit code you can act on.
</human-check>
</verification>

<success_criteria>
1. `deform6 inspect corpus/vb6-code/Mandelbrot/Mandelbrot.exe` prints eight lines with the labels File, Format, Runtime, Header, Project, Title, Mode, Objects, exits 0, and a before-and-after set of path, length and modification time for that directory is identical. This is ROADMAP criterion 1.
2. `cargo test -p deform6 --test corpus_sweep` passes. The sweep asserts 44 files before it loops, and for each of the 44 it observes the runtime DLL name and the four signature bytes as values carried out of the file, not as a single-variant enum. This is ROADMAP criterion 2.
3. `cargo test -p deform6 --test refusal` passes with nine tests, of which one is the control. A VB5 fixture, an empty file, and a fixture whose data directory 14 is set give three results that differ from one another and from the plain no-runtime case, which is ROADMAP criterion 3. Making `has_clr_header` return a constant `false` makes exactly one of them fail.
4. `cargo test -p deform6-cli --test cli` passes, and two of its tests prove a usage error exits 5 rather than 2.
5. Every refusal path writes exactly one non-empty line to stderr and nothing to stdout, which is DET-04's one clear sentence made mechanical.
6. `grep -rnE '(Cli::parse|process::exit)[[:space:]]*\(' crates/deform6-cli/` prints nothing.
7. `grep -rnE 'MSVBVM60|VB5!' crates/deform6-cli/src/` prints nothing, so the Runtime and Header lines print values the report read from the file rather than literals in the source.
8. `git status --porcelain corpus/` is empty after the full suite runs.
9. `cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace` exits 0 on a clean clone. This is ROADMAP criterion 5.
10. The SUMMARY records eight deliberate breakages and the outcome of each, including the two that produced no failure and what was added to make them fail.
</success_criteria>

<output>
Create `.planning/phases/01-it-reads-the-file/01-08-SUMMARY.md` when done.

It must carry three things Phase 6 reads to write the released documentation:
the observed eight-line output of one real run, pasted verbatim; the statement
that all 44 corpus programs are native so the P-code branch is untested by
construction; and the statement that the VB5, VB4 and .NET refusals are proved
against fixtures patched in memory and not against real binaries of those
kinds, with the reason.
</output>

---
status: complete
phase: 05-hostility
source: 05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md, 05-04-SUMMARY.md, 05-05-SUMMARY.md, 05-06-SUMMARY.md, 05-07-SUMMARY.md, 05-08-SUMMARY.md
started: 2026-09-13T21:11:01Z
updated: 2026-09-13T21:25:19Z
---

## Current Test

[testing complete]

## Tests

### 1. [05-01] Severity::Tolerated added, eight DefectKind arms reclassified, and a corpus wide test proves eve
expected: Severity::Tolerated added, eight DefectKind arms reclassified, and a corpus wide test proves every defect the 44 vendored programs raise is Tolerated
result: pass
source: automated

### 2. [05-01] deform6::inspect refuses a Recoverable defect by default (exit 4, names the byte offset); --salv
expected: deform6::inspect refuses a Recoverable defect by default (exit 4, names the byte offset); --salvage on inspect continues past it and produces the report
result: pass
source: automated

### 3. [05-01] --salvage on extract writes the project and a report naming every assumption with its byte offse
expected: --salvage on extract writes the project and a report naming every assumption with its byte offset; a strict report names none
result: pass
source: automated

### 4. [05-02] GuiTable::walk bounds wFormCount against its own mapped region before the loop starts; a 4096 by
expected: GuiTable::walk bounds wFormCount against its own mapped region before the loop starts; a 4096 byte image declaring 0xFFFF forms gives exactly one entry and one ImplausibleCount defect with a maximum of 1, and allocates nothing from the declared count
result: pass
source: automated

### 5. [05-02] Every capacity sized allocation under crates/deform6/src/ is traced to internal or bounded, writ
expected: Every capacity sized allocation under crates/deform6/src/ is traced to internal or bounded, written down in an audited list, and a new unaudited one fails the gate
result: pass
source: automated

### 6. [05-03] crates/deform6/fuzz generated by cargo fuzz init (not hand written), excluded from the workspace
expected: crates/deform6/fuzz generated by cargo fuzz init (not hand written), excluded from the workspace by the root Cargo.toml's pre-existing exclude line, with the one target parse.rs driving deform6::inspect (Mode::Strict, then Mode::Salvage) and deform6::write::project (Mode::Salvage) over fuzzer bytes, asserting nothing about either result
result: pass
source: automated

### 7. [05-03] cargo run -p xtask -- fuzz-pr and fuzz-cron are the only place a cargo fuzz call appears in this
expected: cargo run -p xtask -- fuzz-pr and fuzz-cron are the only place a cargo fuzz call appears in this repository; both name --fuzz-dir, -rss_limit_mb=2048 and -detect_leaks=0 explicitly, fuzz-pr bounds by -max_total_time=60 (roadmap success criterion 1) and fuzz-cron bounds by -runs=500000, chosen against the measured leak size with a safety factor of roughly 18
result: pass
source: automated

### 8. [05-03] .planning/WINDOWS.md finding 14 records the crate::error::damaged leak: its measured size, the r
expected: .planning/WINDOWS.md finding 14 records the crate::error::damaged leak: its measured size, the refusal path that now reaches it, the two places that work around it, the three costs, the fix not taken, and what would reopen it
result: pass
source: automated

### 9. [05-04] fuzz-pr: a bounded pull request job that installs the pinned stable toolchain and a second night
expected: fuzz-pr: a bounded pull request job that installs the pinned stable toolchain and a second nightly toolchain, installs cargo-fuzz 0.13.2, seeds crates/deform6/fuzz/corpus/parse from corpus/ (44 files) and crates/deform6/tests/regressions/ (1 file), and runs cargo run -p xtask -- fuzz-pr with no fuzzer flag of its own
result: pass
source: automated

### 10. [05-04] fuzz-cron: a second job, triggered by a daily schedule only, seeded the same way, calling cargo 
expected: fuzz-cron: a second job, triggered by a daily schedule only, seeded the same way, calling cargo run -p xtask -- fuzz-cron (the iteration bounded subcommand) and no other job of the two does
result: pass
source: automated

### 11. [05-04] Neither job names a fuzzer flag of its own (no -rss_limit_mb, no -runs=, no gate command) and th
expected: Neither job names a fuzzer flag of its own (no -rss_limit_mb, no -runs=, no gate command) and the toolchain pin is unchanged
result: pass
source: automated

### 12. [05-05] A hostile GUI-table-shaped PE image built from literals this repository owns, with three unit te
expected: A hostile GUI-table-shaped PE image built from literals this repository owns, with three unit tests (exact length, determinism, PE signature) proving its own shape
result: pass
source: automated

### 13. [05-05] The stable replay of every committed regression input through both Mode::Strict and Mode::Salvag
expected: The stable replay of every committed regression input through both Mode::Strict and Mode::Salvage, asserting only that the process survives
result: pass
source: automated

### 14. [05-05] The committed seed's provenance is checkable: a test asserts its bytes equal support::hostile::g
expected: The committed seed's provenance is checkable: a test asserts its bytes equal support::hostile::gui_table_overcount_4k's own output
result: pass
source: automated

### 15. [05-06] A deterministic, seeded, dependency-free mutation sweep over the vendored corpus runs inside car
expected: A deterministic, seeded, dependency-free mutation sweep over the vendored corpus runs inside cargo test --workspace on stable, with no nightly toolchain, exercising both Mode::Strict and Mode::Salvage plus the writer on the salvage result, asserting nothing about the result variant and only that it ran the claimed iteration count
result: pass
source: automated

### 16. [05-06] PathIssuer and SafeNameIssuer no longer scan a growing Vec for every issued path or name, and no
expected: PathIssuer and SafeNameIssuer no longer scan a growing Vec for every issued path or name, and no longer restart their collision-suffix search from the first candidate on every call, closing the O(n^2) denial-of-service defect the sweep found
result: pass
source: automated

### 17. [05-07] corpus/manifest.toml format and the fetch-corpus command: validates every entry before any fetch
expected: corpus/manifest.toml format and the fetch-corpus command: validates every entry before any fetch, refuses a manifest below the minimum entry count, hashes with sha2, and fails the whole command on the first failed fetch or hash mismatch
result: pass
source: automated

### 18. [05-07] pin-corpus: fetches an address once, computes the SHA-256, appends the entry, refuses a duplicat
expected: pin-corpus: fetches an address once, computes the SHA-256, appends the entry, refuses a duplicate name, an unsafe name, and an insecure address before it fetches, and preserves the header comment across a rewrite
result: pass
source: automated

### 19. [05-07] Three real programs (map-editor-2d, passgen, transparency-2d) pinned and fetched over the live n
expected: Three real programs (map-editor-2d, passgen, transparency-2d) pinned and fetched over the live network, hash-verified twice in a row, with corpus/fetched/ left untracked; every pinned hash cross-checked independently against the vendored copy of the same program with shasum -a 256
result: pass
source: automated

### 20. [05-08] Three independent counts (vendored corpus excluding corpus/fetched/, the fetched robustness set,
expected: Three independent counts (vendored corpus excluding corpus/fetched/, the fetched robustness set, the regression directory), each checked against its own true source (the pinned 44, corpus/manifest.toml's own table count, and a stated minimum), with an absent fetched directory reported as absent and never as a verified zero
result: pass
source: automated

### 21. Cold start with no fetched corpus
expected: On a fresh clone corpus/fetched/ does not exist, because its bytes are never committed. cargo test --workspace still passes: the no panic proof run reports the fetched set as absent rather than as zero files, and the gate is green.
result: pass
source: automated
evidence: corpus/fetched/ moved away, cargo test --workspace exit 0, 970 passed 0 failed, then restored with git reporting no change under corpus/.

### 22. The count assertion refuses an emptied regression directory
expected: Removing every file from crates/deform6/tests/regressions/ makes cargo test --workspace fail, and the failure message says the directory must never be emptied because a loop over an empty directory passes while proving nothing.
result: pass
source: human

### 23. The no panic proof run, and what panic = abort does not prove
expected: Every input from all three sources, 48 in total, runs through Mode::Strict and Mode::Salvage, write::project runs on every successful salvage, no process aborts, and the printed count equals the number of files that exist. The run proves no panic in both the test and the release profile. It does not prove abort semantics, because Cargo compiles every test target with panic=unwind whatever [profile.release] says.
result: pass
source: human
note: The human accepted the narrower proof. The abort path stays unproved on stable, because Cargo gives a test target panic=unwind so libtest can catch each panic. An integration test that starts the release binary as a separate process and reads its signal is the way to prove it, and no plan in this phase does that.

## Summary

total: 23
passed: 23
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None. Every test passed.

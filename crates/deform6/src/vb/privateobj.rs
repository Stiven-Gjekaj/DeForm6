//! Plan 02-03 fills this file with `ObjectInfo`, `PrivateObj` and the
//! procedure name walk: a null entry in `lpProcNamesArray` means a private
//! procedure, and a null `lpProcNamesArray` pointer means the `.bas` module
//! case, which is a different fact (`STRUCTURES.md` section 5.1, section
//! 6.1).
